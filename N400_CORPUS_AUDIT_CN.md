# N400 三目录 SHA 全量还原审计

## 范围与边界

- 来源目录：`2069N-N400-NM-1350-0011-0`、`0012-0`、`0013-0`。
- 输入：`386` 个 SHA；同名 PDF 仅作为视觉验收参考。
- 输出：`646` 个物理 SHA `Sheet` 的 SVG、trace 和 PNG 对照工件。
- 还原原则：SVG 中的几何、文字、图框和页序均来自 SHA；PDF 没有被用来提取
  坐标、文本、矢量或反写任何 SHA/SVG 数据。

## 全量结果

| 项目 | 结果 |
| --- | ---: |
| 成功读取 SHA | 386 / 386 |
| 成功还原物理 Sheet | 646 / 646 |
| PDF 页数与 SHA 物理 Sheet 数一致 | 386 / 386 文件 |
| SHA 标题栏可唯一恢复实际页序 | 386 / 386 文件 |
| SHA trace 可用 | 646 / 646 页 |
| 未闭合的复杂节点候选 | 25 / 646 页（3.87%） |

此前把标题栏的“总页数”按二进制记录顺序当作“当前页码”，造成 `61` 个多页
SHA、`148` 个物理 Sheet 走了不可靠的回退排序。修复后以标题栏纸面 x 坐标选择
字段：当前页号 `x≈0.753`，总页数 `x≈0.773`；所有 `386` 个 SHA 都恢复为连续且
唯一的 SHA 页序。

## 已解决的共性问题

| 优先级 | 问题 | 频率 | SHA 根因 | 处理结果 |
| --- | --- | ---: | --- | --- |
| 1 | 多页图页顺序不可靠 | 61 文件 / 148 页 | `logical_page()` 把两个数字按记录顺序混读 | 按 SHA 标题栏固定纸面位置读页号；现为 0 个回退页。 |
| 2 | 右侧标题栏重复图号，少数页变成巨字 | 646 页均有一对普通/星号图号；其中 3 页发生巨大 PSM 碰撞 | Sheet 同时保留普通图号和最终 `*图号*`；低位 PSM 引用可命中页面容器 | 只保留最终星号图号；星号标题按最近 SHA 锚点选择 PSM 字框。 |
| 3 | `PIPING ISOMETRIC` 标题偶发放大 | 4 / 386 SHA | 固定 PSM `0x0E77` 同时命中容器与真实字框 | 从 SHA PSM 候选中选择最小的真实字框。 |
| 4 | 公司名覆盖底部标题栏 | 3 / 386 SHA | 模板公司名的兄弟 PSM `0x0BB0` 先命中页面容器 | 以 Sheet221 公司名锚点选择最近 PSM 字框；`Adisseo` 字段未发现碰撞。 |

修复后重新还原全部 `646` 页、重新栅格化 SHA SVG，并以原 PDF 验收缩略图复算差异。
此前位居差异榜首的标题栏大字页面不再出现。

作为粗粒度验收排序指标，最大缩略图结构差异由修复前的 `0.03354` 降至
`0.01406`（约 `58%` 降低）。该指标只用于排序人工复核页，不能作为 PDF 反推
或修改 SHA 几何的依据。

## 剩余问题

### 复杂节点细线：25 页（3.87%）

以全量 SHA trace 的线段数量为统计基础，阈值为中位数 `2137` 加四倍 MAD
（`4987` 条线）。共有 `25/646` 页超过该阈值。视觉上差异集中在法兰、阀门、
仪表及密集分支附近，表现为比 PDF 更密的局部细线或轮廓重叠。

这些线可追溯到 SHA 的 `18/32` 两点记录、type-5 复合记录以及 PSM 父子对象组。
同一对象组同时包含真实构件轮廓，因此不能仅凭“线多”或 PDF 观感批量删除。该项
保留为下一阶段的定向解码任务，而不是进行破坏性的全局过滤。

### 细小文字/局部布局差异

对照板中仍可看到少量小字号文字、引线与复杂节点局部位置差异。它们没有出现新的
全页级遮挡或页序错误，且当前尚无可在多种 SHA 中复用的单一二进制规则；本轮不以
牺牲正确构件为代价做猜测性修复。

## 可复跑流程

```bash
python3 audit_sha_pdf_corpus.py <三个N400目录> --out-dir output/n400_corpus_audit
NODE_PATH=<playwright-node_modules> node render_svg_tree_png.cjs \
  output/n400_corpus_audit output/n400_corpus_audit 1200
python3 summarize_sha_render_corpus.py \
  output/n400_corpus_audit/audit_manifest.json
python3 measure_sha_pdf_visual_difference.py \
  output/n400_corpus_audit/corpus_quality_summary.json
```

`build_sha_pdf_visual_qa.py` 生成的对照板左侧是 SHA-only SVG，右侧是 PDF 视觉参考。
对照板用于发现问题和验收，不得作为 SVG 或 SHA 的数据来源。
