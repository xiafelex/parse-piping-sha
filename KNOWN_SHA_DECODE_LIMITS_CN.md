# 已登记的 SHA 解码限制

## Composite `primitive_type=0` 路径

状态：核心语义已验证，不进入当前默认渲染器。

现象：第一批十个 `SHA + PCF` 样本中，`2` 个法兰、`2` 个阀门、`1` 个弯头和 `1` 个仪表可以通过 `UCI -> graphic_ref -> PSM` 找到页面位置，但附近没有已解码的直线、圆弧或椭圆图元。

已确认：`primitive_type=0` 不是最终可见轮廓，而是复合符号的范围/起始记录。对第一轮十个 SHA 的 SHA-only 复核（`3,803` 条 type-0 子记录）发现其稳定序列为：

- `0-5-5-5`：`2,435` 个父对象，type-0 的四个坐标精确等于后三条 type-5 可见线的联合外包范围。
- `0-6`：`110` 个父对象，type-0 的范围精确等于后续 type-6 圆弧的范围。
- `0-5-5-5-5`：`838` 个父对象。type-0 精确包住紧随的前三条 type-5 构件轮廓；第四条 type-5 是从该符号接出的连接/引线，故不属于该局部范围。
- `0-0-0`：`134` 个父对象，仍是嵌套范围类记录，当前没有可单独渲染的路径证据。

因此，直接将 type-0 的四个数值绘制成矩形或线段会生成错误几何；默认渲染器忽略它是正确的。其作用可以用于以后恢复复合构件的局部包围盒、命中测试和同一符号内的子线归组，但不能作为 SVG 可见路径。

### 当前处理决定

用户所称的 `type=-` 问题此前统一登记为“复杂复合路径 / `primitive_type=0`”。现在已经确认 type-0 是非绘制范围记录，不再是待补画的构件问题。仍待专项解码的是复合父对象的可见性/层级关系，以及无法由 type-5/type-6 和已验证的短 `18/32` 线完整解释的小型构件细节。

已排除的错误做法：

- 不把 `type=0` 的范围直接渲染为矩形。
- 不用 PDF 图元或坐标反推 SHA。
- 不把 PCF 三维坐标直接投影到 ISO 图纸。

后续解码方向：以 type-0 的局部范围作为非绘制分组证据，继续解开 PSM 父子可见性关系和少量 `18/32` 格式变体。PCF 仅用于确认构件类别；位置、尺寸、朝向必须仍来自 SHA。可复跑诊断器：`analyze_composite_primitives.py`。

## Composite `primitive_type=2` 路径

状态：核心引用关系已验证，不单独进入默认渲染器。

最初十样本的后续物理页完成了原始 `18/32` 全量扫描后，type-2/type-11/type-16 的关系已先得到强证据；当时观察到的 `14,000` 条 type-2 都精确别名同页 `18/32` child reference，复合对象引用 `+6` 也精确等于该线的 graphic reference，其中包括 4 条零长度点记录。该结论的适用范围仅为当时十样本；后续 459-SHA 全库补充的四条 type-2 小圆别名见下一段。
- type-11/type-16 都有两种已知附属形式：对应 `18/32` 线，或对应 `0x0061` PIPE 圆弧；两类的复合对象引用都等于对应图元的 graphic reference。后续样本中补获的 type-16 圆弧例子（如 `5848/6207/6572`）证明该规则不能只写成“type-11 圆弧”。

这些 child 的短小范围会沿管道中心线和其他图元形成密集序列，看起来像虚线/分段笔划；但范围坐标与最终线端点不处于同一已解码坐标语义，不能把它直接画成矩形或线。扩大到全库 `459` 份 SHA 后，type-2 的 `101,509` 条别名到 `18/32` 线，另有 `4` 条别名到 `0x0059/0x002B` 小圆；复合对象引用 `+6` 在每一条都精确等于被别名图元的 graphic ref。type-11 全部为 `4,617` 条 `18/32` 线加 `688` 条 `0x0061` 圆弧，type-16 全部为 `1,175` 条线加 `176` 条圆弧，均为零未匹配。type-10 共 `8` 条，全部别名到线；type-21 共 `94` 条，分别别名到 `82` 条线和 `12` 条圆弧，也为零未匹配。因此 type-2、type-10、type-11、type-16、type-21 都不是独立应补画的法兰、阀门或管段轮廓；默认渲染器只画其已经解码的 `18/32` 线、`0x0059` 圆或 `0x0061` 圆弧实体。

`type=0` 也已扩大到同一 `459` 份 SHA 全库验证，不再保留未分类项：共 `47,856` 个 type-0 子项中，`42,160` 个的四个范围值精确等于同一复合记录内某段连续兄弟项的范围并集，因此是局部范围头；另 `5,696` 个满足 `composite parent_ref == 0x004D secondary_ref` 且 `type-0 child_ref == 该文字组的 child_ref`，是文字组范围伴随项。前者包括 `0-5-5-5`、`0-5-5-5-5`、`0-6` 等符号/圆弧组合，后者包括常见的三行 `E/N/EL` 坐标文字组以及单行/双行注释。它们都不是额外应渲染的线、法兰或仪表；保留作 SHA 内部组与范围追溯即可。

### 已纳入的 type-2 短线变体

短 `18/32` 记录只有在其 child ref 同时出现于结构完整的 type-2 复合记录时才加入默认 SVG。这保留了全局阈值对二进制误判的保护，又恢复了连接节点、法兰/仪表附近的微小线细节。零长度记录则仅进入层级计数，不绘制为线。PDF 仅用于视觉验收，未提供任何新增坐标或路径。

## PCF/SHA 候选关联

覆盖审计中，动态属性的 `graphic_ref` 仅以四字节小端序列出现在某个 `Sheet` 内时，不能作为构件已经绑定到该页面图元的证据。它可能刚好命中复合坐标字段。审计报告现在将此类条目标记为 `candidate-sheet-byte-occurrence`；它只用于排查，不能用来决定某个 type-0/type-2 子图元属于该 PCF 构件。提升为直接关联前，必须有已解码的 Sheet 引用字段或已验证的 PSM 父子关系。

## PSM `relation=201` 到复合构件轮廓

状态：已作为 SVG 追踪映射使用，不改变可见几何。

十样本中，部分动态 `graphic_ref` 直接等于完整解析的 `PSMspacemap/0x00008000` 节点 ID。关系码 `201` 的子引用可精确命中同一物理 Sheet 的 type-0 范围子记录；而 type-0 的同父 type-5/type-6 子项已被独立解码为构件轮廓。因此可建立下列严格链路：

`UCI -> dynamic graphic_ref -> PSM node (relation 201) -> type-0 -> same-parent visible children`

第一轮十个 SHA 中，3 个 SHA 命中该链，得到 `124` 个可见 child/UCI 映射。渲染器只把它写入 SVG `data-uci` 与 trace 的 `mapping_basis=direct_psm_relation_201`，不根据它隐藏、移动或新增任何线。关系码 `190`、`183`、`184` 目前仅能落到 Sheet 本地编号区间，尚未命中已解码图元边界，仍是候选，禁止推广。

进一步核对发现，十样本中 `190/183/184` 的 `88/89` 个候选 child ref 完全不以四字节形式出现于其区间所属 Sheet；因此它们不是“还没识别的 Sheet 线段/文字/椭圆”，而是 PSM 自身的内部层级或容器引用。`PSMroots`、`PSMclustertable` 已确认是名称/流注册表，不是构件几何索引；后续应转向 `PSMspacemap/0x00000000` 的类型特定中段布局及短空间表的语义，而不能用 Sheet 扫描或 PDF 位置猜测。

`PSMspacemap/0x00000000` 已取得有限但可重复的边界证据：十份 SHA 都有零结束的 type-2/type-3 前缀（9 份为 5 条，AMSS1 为 3 条），尾部还有独立 type-3 根块（1 或 2 个 child，关系为 `190` 或 `201`）。两段均可按原始字节边界读取；中间大量 type-1 记录不是 `0x8000` 的 `<4H> + <IH>` 布局，不能继续按该格式解析。`0x2000/0x4000/0x6000` 则是 `tseg + 4 个 uint16 头字段 + uint16 列表` 的精确长度格式，其中第 4 个头字段等于列表长度；值的业务含义仍未知。

完整全库复核后，`0x0000` 还存在第二个已定界的“长前缀节点表”变体，不能再统称为未解析：`63/386`
份 SHA 可精确读取为 `tseg + <节点数、原始标志、原始值、前缀 uint16 数> + 前缀 + N 个 <4H> + <IH> 子项`。
其中节点数为 `422–1,706`，前缀长度为 `2–778`；`62` 份在节点表后有 `6` 字节零填充，另 `1` 份没有填充，均可完整消费。该表的节点和关系边界已确认，但前缀值、局部 child ref 与关系码仍只能定义为 PSM 内部路由，不能当成 Sheet 图元、UCI、法兰或绘图顺序。剩余 `323` 份使用零终止关系容器变体，应走其独立解析分支，不能因前缀字段为零而判为损坏。

对这 `63` 份长前缀表的关系目标进行全量命名空间复核后，`190` 的 `21,657/21,657` 条边均精确指向
`Unclustered Dynamic Attributes` 中的 `0x0089` 属性引用，因此可确定为“节点到动态属性”的路由；它不是
图元可见性或构件归属。`181` 共 `5,486` 条，其中 `5,360` 条精确命中物理 Sheet 根，其余 `126` 同时仅
有 PSM 包络命中，故在此表内可称为“页根/布局锚点路由”，但不能推广成所有 PSM 表中的通用含义。`182`
的 `5,485/6,116` 条边直接命中 JSite 资源；`183` 的 `5,922/11,371` 条边命中 PSM 图框细边包络，二者均有
稳定的布局/放置用途但不是构件可见性。`184` (`444`) 仅落入 `0x0000` 内部空间；其稳定终端链在下文说明。
`201` 的原始目标也没有到动态属性、UCI、JSite 或命名图层的直接绑定，但随后已通过 Sheet 几何伴随记录完整
闭环，不能再归为未命名。

因此报告仅在“长前缀 `0x0000` 表且已完整消费”的作用域内标记：`181 = physical-sheet-root-or-layout-anchor-route`、
`182 = jsite-resource-placement-route`、`183 = template-frame-or-layout-anchor-route`、
`184 = shared-internal-terminal-anchor-route`、`190 = dynamic-attribute-route`、
`201 = geometry-companion-route`。`184` 的 `444/444`
条边通过直达或单中继路径最终都收敛至内部目标 `2402`，其业务名称仍未知，但已可排除为构件/可见图元。
这些不是 Shape2D 全局 opcode 定义；`201` 虽可用于伴随几何交叉校验，仍不驱动 SVG 的新增、删除或可见性判断。

随后对同一长前缀表的 `201` 专项交叉验证，现已完整确定其在此表内是“几何伴随路由”：`5,169` 条目标中，
`4,974` 条精确命中 `0x0013/0x00AC` 线几何伴随 primitive，且 `4,974/4,974` 都可按同 graphic、同图层、
端点正向或反向完全一致回连至 `18/32` 线；另 `195` 条精确命中 `0x0013/0x0063` 圆伴随 primitive，`195/195`
均可按同 graphic、同图层、同圆心回连至 `0x59/0x2B` 椭圆。只有 `91` 条恰好满足 primitive `+1`，故 `+1`
不是跨导出格式的绑定条件。这个结论只适用于完整消费的长前缀 `0x0000` 表；它不赋予 UCI，也不新增 SVG 图元。

对 `0x2000` 的十样本补充统计：列表值均无重复，最小值均为 `1054`，最大值均等于第 3 个头字段减 `2`。这说明该表至少携带一个受上界约束的编号集合，但尚不能证明编号是 PSM 节点、Sheet 图元、构件或图层，禁止把这一规律用于出图关联。

### PSMroots 根目录与短表名称

`PSMroots` 已可完整按原始字节读取：`rootb`、8 字节头部，随后重复
`uint32 字符数 + UTF-16LE 名称 + uint32 根引用`。十份样本均得到五项：

- `TopVFSet -> 0x10BB`
- `_SupportOnlyList -> 0x4000`
- `Dynamic Attributes Set Table -> 0x2000`
- `StyleLibrarian -> 0x0001`
- `DocStore -> 0x0000`

头部中的字节值为 `9`，但其后实际是五项，故它只能记作原始
`header_count_byte`，不能误称为条目数量。由此可将 `0x0000`、`0x2000`、
`0x4000` 的根名称分别确定为 `DocStore`、动态属性集合表和仅支撑列表。
这只是目录命名，不证明它们是可见几何、图层或构件类别。

`PSMsegmenttable` 也已补全段位关系：其格式为 `stab + uint32 payload_count + payload bytes`，
第 `i` 个 payload 字节精确对应 `PSMspacemap/0x(i*0x2000)` 的段标签；不存在该空间表的
尾部槽位为 `0`。十份样本中 `0x0000` 至 `0xC000` 均符合该对齐，包括可选的
`0xA000/0xC000`。标签 `1/9` 的具体类型、可见性或图元语义尚未确定，不能据此过滤
或新增 SVG 图元。

### `0xA000` / `0xC000` 扩展空间段的属性路由证据

在 N400 语料中，`0xA000` 出现于 10 份原始 SHA，`0xC000` 出现于其中 2 份。二者都可完整按
`<4H> + <IH>` 节点表消费，且并不是法兰、阀门、焊缝或线段的额外图元表。对全部关系目标按已经
定界的命名空间复核后，`relation=190` 出现了无例外的精确关系：`0xA000` 的 `7,048/7,048` 条，
`0xC000` 的 `1,159/1,159` 条，均指向 `Unclustered Dynamic Attributes` 中的 `0x0089` 动态属性引用。

因此当前可把这两段稳定称为“动态属性路由的扩展层级表”。它们仍含 `182/183/184/201` 等关系，
但这些目标没有形成到 Sheet 可见图元的直接边界；特别是不能因某个 `object_ref` 命中命名层或 PSM
节点，就把管道轮廓自动归为 `PIPE/FITTINGS/WELDS`。报告保留逐关系的 Sheet 根、命名对象、动态
图形、动态属性、JSite 和 PSM 包围盒命中计数，供后续继续解码；SVG 默认不根据这些关系增删图元。

进一步对这些关系目标执行 `target & 0xFFFFE000` 分段验证，扩展表中的所有已读取关系目标都能落入
本 SHA 实际存在的 `PSMspacemap` 段。也就是说它们是“段基址 + 段内偏移”的地址，不应与节点 ID
混淆。典型的 `0xA000` 模式为：`183 -> A000`、`201 -> A000`（少数跨到 `C000`）、`190 -> 6000`
（后者同时已确认是 `0x0089` 属性区）；`182/184` 则在 `0000` 和当前扩展段之间路由。该结论解释了
为何 `201` 不命中节点表：它指向段内地址，而不是节点编号；它仍不能单独定位某个 Sheet 图元。

`PSMcluster0` 中还解出一类命名记录：`uint16 0x0081 + uint32 记录长度 + 5 个 uint32
字段 + UTF-16LE 名称 + 终止符`，且记录长度严格等于 `30 + 2*(字符数+1)`。十份样本均
可恢复 `PIPE`、`FITTINGS`、`WELDS`、`DIMLINES`、`MATLIST`、`ISOTEXT`、`SKETCHES`、
`NOZZLES`、`Level n` 等命名层，以及每条的对象引用和条目编号。这证明 PSMcluster0
内含图层/层级名称表。第三个中间 uint32（解析字段 `field_3`）现已解码为本页该图层对象
的成员记录总数：十样本后续页的 `3,404` 个层对象中，全部与已解 `18/32`、`4d`、
`13/AC`、`59/2B`、`61`、`13/63` 记录按 `page_layer_ref@14` 汇总的数量精确一致，
其中包括不绘制的零长度 `18/32` 点记录和空 `4d` 对象。`field_1`、`field_2` 在十样本
所有命名记录中恒为 `0`，可仅标记为当前导出中的保留/未使用字段；计数也不等于可见性或
绘制顺序，不能据此改变 SVG 几何。

部分命名记录的 `object_ref` 还能精确命中完整 `0x8000`、`0xA000` 或 `0xC000` 空间表
中的节点 id，现已在层级报告中保留这条 `命名层 -> PSM 节点` 的直接链路。复杂多空间表
SHA 中此链路较多，精简变体可完全没有；但节点 child 到 Sheet 可见图元的成员关系仍未
证明，禁止据此自动给 SVG 线段赋 `PIPE/FITTINGS/WELDS` 层名。

对已命中的命名层节点进一步统计后，复杂样本中的关系 `190` 会稳定大量指向已经验证的
`0x0089` 动态属性引用；关系 `183/184/201` 仍指向未解目标。层级报告现会输出这些
分类计数，因此可证明层名参与动态属性层级，但仍不能证明某条可见线段属于该层名。

命名记录总数还给出了一条严格的多页校验规律：第一批十个 SHA 全部满足
`0x0081 命名记录数 = 175 + 92 × (PSMclustertable 中 Sheet221 后的 Sheet* 数量)`。
这里的 Sheet221 后 Sheet* 数量是 Sheet6 首图页之后的附加物理页数，而不是整份 ISO 的
总页数；例如 SCC 为 `175 + 92×7 = 819`，DMW 为 `175 + 92×10 = 1095`。该规律可仅用
SHA 验证多页导出是否完整；175 与 92 目前是已观察到的固定基础/每个后续页组大小。

固定前 `175` 条也不再是无法归属的全局名称表：它们是 `Sheet6` 首页的 `page_layer_ref@14`
直接目标。十样本中全部 `23,592` 条已解 Sheet6 记录均命中该组；每份 SHA 都有 `164/175`
条 `field_3` 与首页成员数量精确一致。剩余固定 `11` 条（`Default`、若干数字层、`Border`、
`DwgTemplate`）带有数量但没有 Sheet6 成员，应保留为模板/默认基础对象，不能强行当成可见
首页图层。层级报告单独输出 `sheet6_base_named_layer_group`。

再把共享 `Sheet221` 中保持 `+14` 基础层引用的四种有界记录（`0x0018`、`0x004D`、
`0x003D`、`0x0084`）合并后，十个样本每份都达到 `174/175` 条基础对象的 `field_3`
精确匹配。唯一例外为 `Default object_ref=0x0008`：它不出现于任何 Sheet 图元，只出现
在 PSM/文档元数据上下文，应标记为非绘制默认/容器对象。报告中的
`shared_base_named_layer_group` 保留这项合并统计；模板中尚未解出的资源成员不能据此生成
几何。

对 `Sheet221` 的模板成员又完成了两项有界识别。十份样本均有两条 payload 为 `234` 字节的
`0x003D` 记录，均落在 `Border` 层；它们的 PSM 范围分别稳定对应两个内嵌位图资源
`JSite690` / `JSite1402` 的放置区域。现已确认其 `uint32@162` 直接给出位图资源号，分别为
`690` 与 `1402`；因此可将其标记为“位图放置包装记录”，但不能把它误称为位图本体或画成线。其
归一化放置原点 `<2d>@42` 与宽高 `<2d>@82` 也已直接解码：两个位图框的宽高比分别精确等于其
`537x212` 与 `866x498` 像素比，故 SHA 本身足以确定嵌入资源的二维放置框。其余矩阵字段仍保留原始值，
不可据此虚构旋转或裁剪。每份还有一条 payload 为 `104` 字节的
`0x0084`，落在 layer `60` 且 PSM 范围为页面级，应标记为模板页/容器。386 份 N400 SHA 每份
都恰有一条，五点坐标也完全一致。其从 `+30` 开始是
五个 `<double x, double y>` 点，首尾相同，已经可还原为闭合的归一化页面模板路径；它不是工艺
构件，也不能与 StyleCluster 中同标签的局部符号多边形混用。两种记录的 `+20` 只是原始头字段，
不是已解码的 style。

`PSMspacemap/0x00000000` 的标准 counted-node 变体中，零关系锚点不再只能笼统标成布局包络。
全库有 `323` 份此变体：每份都稳定引用 `Sheet221` 的六个已解码模板对象，即五条
`0x0018/0x0032` 边界/分隔线（`3242,3845,3870,4367,4396`）和闭合页面路径 `4451`。其中
`169` 份只有这六项，另 `154` 份还多一个已验证的物理 Sheet 根。报告将前者标为
`sheet221-template-primitive`，后者标为 `sheet-content-root`；两者均不是管件或 UCI 构件。

长 `Sheet221 0x004D` 也拆成两种：普通有界形式从 `+30` 开始直接存放可见 UTF-16 文本，
末尾为文字变换；Revision 子类型则在短的未知前缀后保存 UTF-16 XML `intstgxml` 绑定表达式，
并在记录尾部保存文字变换。解析器现会输出 XML 的 `stream/select/alt`、XML/变换相对偏移，及
未知前缀的原始 hex/`uint16`，而不会虚构其含义。全库 386 份 SHA 的 9,650 条此类记录均指向
`Revision` 流；`select` 只取六类字段 `MajorRev_ForRevise`、`RevisedBy`、`RevisedDate`、
`CheckedBy`、`ApprovedBy`、`RevisionDescription`，但会指定不同历史行（`[1+n]` 或 `last()-n`），
因此可直接还原修订栏的多行字段绑定。若目标历史行或字段不存在，绑定 XML 的 `alt` 属性就是
显式回退文字（本库通常为空串），应输出为空格位并标为 `resolved-alt-*`，而不是误报记录坏掉。
常见八字节前缀是稳定的版式区分信息，但其业务语义尚未单独证实。固定说明文字的方向字段可为
`0.001` 一类的字号/比例，而不是单位方向向量，必须
原样保留；后者定义“图框字段从 Revision 流取哪个值”，不应将 XML 本身绘制到 ISO。两类均保留
`child_ref@6`、`sheet_ref@10`、`page_layer_ref@14`、`style_ref@20` 作为 SHA 来源链路。

进一步按对象引用校验后，每个 92 条组已可按顺序精确对应具体物理 Sheet：第 `i` 组对应
Sheet221 后第 `i` 个 `Sheet*`，所有组的名称序列完全相同，且
`组内最小 object_ref = 对应 Sheet header local-id start - 2`。十份样本均无例外。再对
每页有效的 `18/32` 两点线记录复核后，记录起始 `+14` 的 uint32 在十份样本的全部
`65,845` 条可绘线记录及 `23` 条零长度点记录中都精确落入本页这 92 条对象引用：没有
跨页引用，也没有未命中值。因此
该字段已从“局部分组线索”提升为已验证的 `page_layer_ref`，可直接标记为 `PIPE`、
`FITTINGS`、`DIMLINES`、`ISOTEXT`、`FRAME` 等本页层名。这个结论只适用于已验证的
后续 Sheet 的 `18/32` 记录；Sheet6 和其他图元格式仍不可套用。解析报告现在输出这条
`物理 Sheet -> 命名层对象组 -> 18/32 线图层` 映射，能够定位每页的层级对象和线图层。

同样的层字段还出现在普通 `0x004d` 文字记录中。该布局的 `+2` uint32
记录长度严格等于 `60 + 2×(+28 的 UTF-16 字符数)`，文字从 `+30` 开始，随后是四个
double 的文字变换。十个样本的后续页共验证 `4,548` 条，所有 `+14` 的
`page_layer_ref` 都命中本页 92 条层对象，所属名称为 `ISOTEXT`、`DIMTEXT`、
`DIMLINES`、`FRAME` 或 `MATLIST`。因此报告已能对这类文字给出直接层归属；它不表示
该层的可见性、字体或绘制顺序已经解码，不能据此删改文字。

全库另有一个独立的扩展 `0x004D` 文字布局：`record_length = 68 + 2×(+28 字符数)`，文字从
`+38` 开始，普通文字起点 `+30..+37` 的八个字节是额外控制区。它保留相同的 `child_ref@6`、
`secondary_ref@10`、`page_layer_ref@14`、`style_ref@20` 和文字变换字段；全 `459` 份 SHA 中为
`1,383` 条，普通布局为 `123,964` 条。解析器以 `text_prefix_extension_raw_hex` 原样输出这八字节，
其中 `1,383/1,383` 条的 `uint16@30` 恒为 `1`、`uint16@36` 恒等于 `+28` 字符数，且
`uint32@32` 都精确等于同条 `style_ref@20` 通过 StyleCluster 解析出的 `font_style_ref`。因此后两项可作为
显式字体样式冗余校验；`@30=1` 仍只保留为稳定控制常数，不能延伸解释为对齐、可见性或几何规则。

`0x004d` 的 `secondary_ref@10` 也已完成分类，不再是未解字段：十样本后续页全部
`4,548` 条文字中，`4,193` 条等于当前 `Sheet` 数字编号，另 `355` 条精确指向一个完整
`0x007b` 复合记录的对象引用 `+6`。后一类正是同一坐标注释组的共同父对象，例如
`E / N / EL` 三行文字共同指向相邻复合对象。`0x007b` 记录本身满足
`+2 record_length = 32 + 14×(+22 child_count)`，对象引用在 `+6`，当前 Sheet 引用在
`+10`；十样本的 `5,491` 条记录全部满足 `+10 = 当前 Sheet 数字编号`。此前把 `+2`
作为 composite parent 的实现已修正，`+2` 只能解释为长度，不能用作图元归属。

圆形/椭圆的 `0x0059/0x002B` 记录也已接入同一页面图层体系：`+6` 为 primitive reference，
`+10` 为 graphic reference，`+14` 为 `page_layer_ref`，`+20` 为 style reference，中心点
从 `+24` 的两个 double 读取。十样本后续页的 `159` 条有效记录全部命中本页层对象，其中
`144` 条为 `WELDS`、`13` 条为 `ISOTEXT`、`2` 条为 `PIPE`。因此焊缝黑点可以通过 SHA
直接得到 `WELDS` 层证据；但不能把所有圆形一概解释成焊缝，仪表及其他圆形仍需结合其
primitive/PSM 关系分类。

进一步按全库 `386` 份 SHA、`1,032` 张物理 Sheet 做精确 `graphic_ref -> dynamic UCI` 交叉后，
这条 identity 链不只适用于焊缝点：`18/32` 线为 `592,285 / 960,386`，`0x0059/0x002B` 圆为
`5,373 / 5,588`，`0x0061` 圆弧为 `1,405 / 1,624`，`0x0013/0x0063` 圆几何伴随为
`5,332 / 5,374`。只有引用值精确相等时才可将 UCI 写入已有几何；未命中项仍保留其 SHA
坐标和图层来源，绝不能从相邻线、PSM 包络或 composite parent 借用 UCI。

新增的 `0x0061` 是直接圆弧管段记录，而不是文字中的偶然字节：总记录长度为 `65`，
payload 长度固定为 `59`，通用引用字段仍在 `+6/+10/+14/+20`，从 `+24` 起的五个
double 依次为圆心 x/y、半径、绝对起始角和绝对终止角。后一个角不能解释为 sweep：
十样本审计的 `26` 条圆弧按“绝对终止角”计算时，两个端点全部在 `0.04` 页面单位内命中
PIPE 层直线端点；按 sweep 解释只命中 `11/26`。这些圆弧全部属于 `PIPE` 层，其中 `23`
条可由 graphic reference 直接命中动态 UCI，另 `3` 条仍保留 SHA 几何和图层归属但没有
直接动态 UCI。渲染器已按 SHA 圆心、半径和起止角输出 SVG path。
该记录长度为奇数，后续记录可能处于奇数偏移，因此不可用“每两字节扫描一次”的策略。

`0x0013/0x0063` 则是焊缝圆的附属精确几何，不应与长度 `172` 的 `0x0013/0x00AC`
线图层关系混淆。其完整有效形式固定为 99 字节：`+35` 起五个 double 为圆心、半径、起止角，
`+76` 是与圆心相同的锚点，尾部是一条 relation `209`、flag `5` 的 child ref。全库 `5,374/5,374`
条短记录均通过该 child 精确回连一个 `0x0059/0x002B` 圆形，且共享 graphic reference、层和圆心。
primitive reference 恰为圆形 primitive 加一的仅 `5,002/5,374`，故它只能是常见局部编号规律，不能作为
绑定条件。`0x0059/0x002B` 本体固定为 43 字节，`+24/+32/+40` 已直接存储圆心和半径；全部 `5,374`
配对的本体半径与 companion 半径完全一致。因此本体可独立绘制，companion 用于显式关系、锚点和参数一致性
交叉验证；不会因为附属记录存在就额外绘制圆形。

此前仅作为局部分组保留的 `0x0013/0x00AC` 记录现已完成完整边界与几何复核：有效形式固定为
172 字节，`+6` 是 primitive ref、`+10` 是 graphic ref、`+14` 是 page-layer ref，未对齐的 `+35`
起四个 double 是保留方向的线端点对，不是始终递增的 bbox。扩大到全库 `459` 份 SHA 后共有 `47,901` 条，
全部有同 graphic ref、同图层、端点**严格反向**完全一致的 `18/32` 线。它因此是
线图元的“几何 + 页面图层”冗余 SHA 关系，可用于 trace/交叉校验，但不新增可见 stroke，也不能判断隐藏
或打印顺序。仅有 marker 而长度/端点范围不成立的原始字节均视为嵌套 payload 假命中，不能当作记录。

完整 172 字节载荷由 3 个连续的 `0x67 + <4d>` 方向端点子段（起点分别在 `+35/+68/+101`）、共享锚点
`<2d>@133`，以及 `+149` 的固定三成员列表组成。全库 `47,901/47,901` 条均有 3 个 `18/32` child ref，
关系码恒为 `203`、flags 恒为 `[1,0,0]`；全部 `143,703` 个成员与对应 child 线逐段严格反向一致，
没有同向或缺失匹配。这确定它是
“固定三段线组 + 锚点”的几何伴随，不是单线 bbox、额外 stroke 或可见性指令。

锚点本身在 `43,394` 条中均不等于任一端点、线段中点、顶点重心或外包中心；它与轮廓的最小距离约为
`0.000335–0.000735` 的归一化页坐标。当前只能称其为“符号内部锚点”，不能把它擅自用于焊缝引线、文字
引线或外部连接点的位置。

该记录的 `+20/+24/+28` 在已审计物理 Sheet 中分别固定为 `204/1/3`，`+32` 的 uint16 固定为
`0x0102`，`+34` 固定为 `0x67`；它们是本记录族的格式头，而不是 `18/32 +20` 的 line style ref。
`primitive_ref` 与其端点对应线的 child id 没有固定差值，说明二者来自不同对象编号空间；生成或修复
SHA 时不可把它替换为线 child id。

`0x004D` 文字记录在 UTF-16 文本后的四个方向/位置 double 之后还固定保留一个 uint32 尾标志。全库
`108,144` 条为 `0`，另 `386` 条为 `1`，恰好每份 SHA 的 `Sheet221` 中文公司名记录各一条。它不是字体、
位置、对齐或旋转字段（这些分别来自样式、位置和方向向量），当前只应作为原始文字变体标志保留。

十样本中 `0x2000` 的所有非空列表值在数值上都落在 `Sheet221` 本地编号区间；
这只能说明区间重叠，不能证明属于模板页。`0x4000` 在十份样本均为空列表，符合其 `_SupportOnlyList` 根名；
`0x6000` 也为空列表但没有对应根名，语义仍未知。

对 `0x6000` 的进一步统计推翻了此前按数值落入 `Sheet221` 区间作出的临时判断：
第二个 uint16 头字段在十份样本中都精确等于 `Unclustered Dynamic Attributes` 内
`0x0089` 属性引用记录总数，例如 SCC 为 `4762`、DMW 为 `5343`。第三个头字段则
等于该总数加一个小容量预留。全量 386 份复核后的预留分布为 `145`（382 份）、`147`（1 份）、
`168`（2 份）、`175`（1 份）；例外只出现于 SWS4/AMSS1 导出变体。十份列表均为空，因此 `0x6000` 可确定
为动态属性的计数/容量控制记录，而不是 Sheet221 图元编号范围、可见图元列表、
图层开关或几何过滤条件。额外容量的具体配置来源仍未解码。

`Unclustered Dynamic Attributes` 中的 `PipeLine Info` 构件子类型现已可按局部长度解析：
每个对象包含四个独立 `0x1080 + uint16 长度 + flag + 键名 + NUL + 值 + NUL` 块，键名固定为
`PipeLine Reference`、`Fly Text`、`Unique Component Identifier`、`Element Tag`，末尾接
`0x0089` 属性空间引用。因此这四项是同一构件的属性组，而不是四个应直接绘制的文字图元。
十样本中该属性块结构稳定；每个有界 GUID 值及其记录尾部的 `graphic_ref` 均与既有
`dynamic_graphics()` UCI 映射精确一致，报告会输出该逐条校验数。`0x0089` 的长度有时等于当前记录长度减 6，有时为不同的容量/跨记录值，不能单独
作为分段依据；最后一个 `PipeLine Info` 后若直接进入文档级属性，也不能据此强行补全构件记录。
解析报告会同时输出已局部定界的属性组和这类保守限制，现有 UCI 到图元链不依赖该尾部猜测。

`Unique Component Identifier` 与 `Element Tag` 属性块之间还有固定的 18 字节伴随项：
`0x0003 + uint16(18) + "UCI Index" + NUL + uint32`。十样本中每个已定界属性组都符合该布局；
非零值只出现在同一 GUID 对应多条 `graphic_ref` 的情况，已观察到 `0/1`、`0/1/2` 至最大 `5`
的实例序列。全量 386 份复核进一步限定了这一结论：`23,476` 个有效 UCI 组中有 `838` 个重复组，
但只有 `216` 组严格为 `0..n-1`，另有大量重复组的索引均为 `0`（并有 1 组缺失索引）。因此报告只能
把非零 `UCI Index` 作为“重复 UCI 的辅助实例标志”，不能把 `0` 当作全局首实例，也不能把它当作稳定的
一物一码编号、二维坐标、PCF 构件号或直接绘图依据。

对 386 份 SHA 的完整属性块复核后，`PipeLine Info` 的构件属性组共有 `44,363` 条：每一条都有
`PipeLine Reference`、`Fly Text`、`Unique Component Identifier` 三键，`44,348` 条另有 `Element Tag`。
其余 `15` 条全部集中在 `N400P3A-HOSO-N419601-01-0.sha`，逐条都拥有独立有界 `0x0089` 尾部引用，
并非流尾截断。因此解析规则必须允许合法的“三键、无 Element Tag”导出变体，不能伪造空标签或将其丢弃。

同一批样本进一步以 SHA 已解码的物理 `Sheet` `0x004d` 文字作精确字符串交叉核对：每份样本的
非空 `Fly Text` 都是零命中，而 `Element Tag` 大多可命中，管线引用也各有一条命中。故 `Fly Text`
目前只能视为构件规格/描述元数据，不能直接作为 ISO 可见注释；`Element Tag` 是可作为文字身份候选的
值，但文字位置、字体和引线仍必须来自其独立的 Sheet/PSM 记录，不能由字符串匹配补造。

该字符串核对现同时报告“属性记录出现次数”和“不同字符串数”，避免把重复管线号误认为逐构件文字绑定。
全库有界 `PipeLine Info` 记录中，`PipeLine Reference` 的 `28,703` 次非空出现均能匹配物理 Sheet 的
`0x004D` 文字，但这是每张 ISO 仅有的管线号文字被该线所有构件重复引用；不能据此将每个构件都画一条管线号。
`Fly Text` 的 `28,703` 次非空出现仍为零直接文字命中；`Element Tag` 的 `23,985` 次非空出现中有
`22,014` 次可与 `0x004D` 文字精确匹配。后者可作为“存在同名可见文字”的证据，仍不能替代其独立坐标、
样式、引线和图层记录。

`Element Tag` 的可见文字候选现进一步落实到具体的 `0x004D` 图元：`20,850` 条属性记录在同一 SHA 中
恰好只有一条同名物理文字，解析器会输出该候选的 Sheet 流、文字 child/secondary reference、图层、样式、
二维锚点和方向。另有 `1,164` 条对应两条或以上同名文字（分布为 2 条 `515`、3 条 `290`、4 条 `139`、
5 条 `196`、8 条 `24`），以及 `1,971` 条无候选；两类都保留为歧义/无匹配，不能自动把文字归属给 UCI
构件。即便唯一候选也只是定位辅助证据，不证明引线连接关系或一物一码的唯一可见标签。

比文字字符串更强的 UCI 图元绑定现已完成：动态属性中的 UCI `graphic_ref` 与物理 Sheet 已解码图元的
`graphic_ref` 做同 SHA 的完全数值相等连接。旧的“扫描至下一条 PipeLine Info 标记”方法会在每份 SHA 的末尾
把后续元数据中的 ASCII `SP1_` 误读为图形号，产生 `330` 条伪未匹配记录。改用有界 `PipeLine Info + 0x0089`
记录后，全库真实 `28,703` 条 UCI 图形引用均直接命中至少一条 Sheet 图元，未匹配数为零；这是 UCI 到实际
图形成员的直接证据，而非文字相似度推测。
同一图形号可同时命中基础 `18/32` 线、`59/2B` 圆、`61` 圆弧、`4D` 文字组及 `7B` 复合轮廓，原因是复合记录
会组合/复用基础几何。因此解析器输出全部命中族，不能仅凭其中一个族就给 UCI 断言为唯一的法兰、阀门或焊缝类别。

这一有界 UCI 规则已同步应用于实际工具入口：SHA 到 SVG 原型、SHA 焊口菱形写入、PCF 焊口编号与 SHA
映射、以及本地应用的 PCF/SHA 直接关联统计均不再使用旧的 next-marker 扫描。后续任何出图、焊口定位或
一物一码追溯必须使用该有界映射，避免将 `SP1_` 等尾部元数据误视为构件图形。

物理 Sheet `0x004D` 文字记录的末尾 uint32 也已完成全库取值核对：除 `0` 外唯一的值是
`0x01000000`，386 份 SHA 中各出现一次，均在 `Sheet221`，文字恒为“德希尼布化学工程（天津）有限公司”、
样式恒为 `217`、方向为水平。解析器将其标记为 `observed_non_ascii_template_text_flag`，用于追溯该中文
模板文字；这只是已观察到的专用模板标志，不能泛化为所有 Unicode、中文字体或文字编码的通用规则。

StyleCluster 文字样式链已经覆盖全量物理文字：`108,530` 条已解码 `0x004D` 记录的 `style_ref` 均能解析到
字体资源和字号比例，不存在未解析样式。`Element Tag` 的唯一文字候选现同时输出字体名和字号比例，例如可见
标签可直接获得 `Courier New`、`font_size_ratio`、坐标、方向、图层和样式。中文模板样式 `217` 则直接解析为
`SimHei`、字号比例 `0.0035`。不同 SHA 中相同 style ref 可能指向不同的文件内样式资源，必须在当前 SHA
的 StyleCluster 中解析，不能把数值 style ref 当作跨文件字体常量。

大量关系 `190` 所命中的 `_ISO` 也已得到独立的紧凑记录边界：
`_ISO + NUL + 0x0089 + uint32 长度/引用 + 8 字节零 + uint32 候选 graphic_ref + 0xFFFF`。
十份样本中每份恰有一条长度 `36`，其余均为 `30`；候选 `graphic_ref` 在 `PSMcluster0` 中取得包围盒
的比例超过 99%。这足以将它界定为“动态对象到 PSM 图形空间的路由元数据”，但尚不能证明其
对应某条 Sheet 线、文字或法兰，仍不得仅凭该引用产生 SVG 几何。

十份样本中除构件四属性组外，`0x1080` 只各出现一次 `Draw`、`Schematic`、`FileName`、`MuSuStr`。
其中可见显示开关、导出/规格路径和内部字符串配置；它们属于文档级导出设置，不是按构件重复的数据，
也不是 ISO 可见标注或几何，解析时应保留为元数据但排除出 SVG 图元链。

386 份 SHA 的通用 `0x1080` 扫描确认不存在其他键族。这四个文档级块的字节偏移和值完全固定：
`Draw=Show@58`、`Schematic=Hide@73`、`FileName=<PlaceBorderLabel...MYLARD.dgn>@137`、
`MuSuStr=INTH@250`。报告将其单列为动态属性流的导出配置头，避免把它们误当作首个管道构件属性或可见文字。

对 `PSMspacemap/0x00000000` 的完整关系序列再按动态属性和 UCI 交叉统计，关系码的“路由
倾向”已有更强边界：十份样本中 `relation=190` 有 `1,398` 次目标为 `_ISO` 动态属性记录，
并有 `765` 次直接命中 UCI graphic reference；`relation=201` 有 `113` 次直接命中 UCI
graphic reference。相反，`relation=182` 绝大部分目标是物理 Sheet header local-id start，
只有 `22` 次动态属性例外（`_ISO` 或 `Element Tag`）。这证明 `190/201` 主要路由到对象/
动态属性空间，`182` 主要连接页面根；但关系码本身不是线、法兰、阀门或文字图元类型，不能
仅凭它新增 SVG 几何。报告现保留按 relation 的动态属性 key/record-size 计数及 UCI 目标数。

对 `0x8000` 中 UCI 图形节点到 Sheet child 的严格链路再作复合父对象验证后，十份样本共有
`38` 条可直接提升：`UCI graphic_ref -> 已完整消费的 0x8000 节点 -> relation 201 -> 精确的
Sheet composite type-0 child -> 落在同一 type-0 范围内的同父 type-5/type-6 可见兄弟`。这 `38` 条无一
例外都先命中 type-0，且每条都有同父范围内可见兄弟，合计 `112` 个 child ref。因此 type-0 不仅是
“不可直接绘制的范围”，还可确定为这部分复合构件的 UCI 绑定入口。渲染时只能把 UCI 传递到
该同父且落入范围的可见兄弟，不能传给邻近线段、从构件向外伸出的引线或按 PSM 包络猜测的几何。

该规则已扩大到全库验证：386 份 SHA 中共有 `297` 个额外的“可见 child -> UCI”关联，分布在 `23`
张物理 Sheet；全部来自 `relation=201` 命中 composite type-0 后向同父范围内 type-5/type-6 可见
兄弟的传播，直接命中普通可见 child 的次数为 `0`。这进一步证明 type-0 是复合构件的 identity 入口而
非可绘线；传播范围必须限制在同一 composite 的已验证 bounds 内，不能跨到引线、邻近直管或其他构件。

对同一批 `0x8000` 节点中 `relation=190` 的 `96` 条“仅落在物理 Sheet 编号区间”候选，再以
已解码的普通线、`18/32` 线、`0x0061` 圆弧、`0x0059/0x002B` 椭圆和 composite parent reference
逐项核对，命中数为 `0`。因此这些候选当前只能视为对象空间/容器层编号，不能因为编号落入某个
Sheet 的 local-id 区间而升级为构件、文字或图形的父对象。这个负结果同样是出图规则的一部分：
只有 `relation=201` 的精确 child 引用及同父范围验证可传播 UCI。

此前对 `PSMspacemap/0x00000000` 中间段“固定 14 字节 type-1 边”的提升已撤回：完整的
type-2/type-3 关系序列可无缝消费到尾根，这些 type-1 外观字节会在已定界容器负载内部重叠出现，
不是独立记录。即使其数值偶然命中图层、节点或资源号，也不能据此赋予层级语义。报告现仅保留其为
诊断字节模式，不作为图层关系、构件关系或 SVG 几何依据。

对完整 `0x00000000` 关系容器的正式边再作十样本交叉后，常规导出中的页面/模板路由可进一步限定：
`181` 重复地把 `parent_ref=238` 指向共享 JSite `559`；`182` 重复地把同一 parent 指向 `Sheet6` 根；
`183` 的目标集合分别与基础 175 图层组或后续页面 92 图层组大幅重合；稀少的 `184` 则连接共享
`Level 01`/`Level 27`、后续页容器、`Default` 和 `01` 图层。这些都是页面/模板控制关系，不是管道
构件、文字或线段关系；AMSS1 缺少该重复段，不能将其当作所有 SHA 的强制结构。

在 N400 全目录做布局判别后，已确认三种 `PSMspacemap/0x00000000` 骨架。第一种为“计数节点+12
字节空 footer”：头部低位 `uint16` 声明普通 `<4H> + <IH>` 节点的精确数量，节点表后为
`type=3,count=0,repeat=1` 加 6 字节零。第二种是完整混合关系布局。第三种在相同四个 `uint16` 头字段
之后先有一个 uint16 引用表：第 4 头字段严格等于该表项数，表后才是第 1 头字段声明的普通节点表，
最后同样为空 footer。386 份按解析器优先采用完整混合关系结构的最终分类为计数布局 171 份、完整混合布局
152 份、前置引用表布局 63 份；此前
两份“未归类”现确认只是计数布局的双 child 尾根：`182 -> 559` 模板路由与 `190` 动态属性路由并列，
不是新节点格式。至此 386 份均可按已知结构定界。仍必须优先采用完整混合关系序列，不能只凭读到文件末尾
选择计数切分。前置表的数值目前仅可
作为原始命名空间索引输出，不能当作图元、坐标或 graphic reference。此前发现的 23 个单 child 尾根都
落在混合或歧义样本，不能归入确定的空-footer计数布局。

前置 uint16 表的语义也已得到可复核的负向边界：63 份共 26,219 个值无重复、全部小于等于头部第 3 个
uint16 上限，并与全部已解码可见 Sheet 图元的 `child/primitive/graphic/composite/page-layer/style` 引用零交集；
同样不命中 UCI、`0x0089` 动态属性、命名图层、空间表节点或文字样式。因此报告将它标为
`reserved-or-unbound-local-id-inventory`（保留或未绑定本地编号清单），而非构件/页面/图层/坐标列表。
“它就是分配器空闲表”的具体业务名称尚无直接写入操作证据，不能升级为确定结论。

计数节点表内的 6 字节 payload 不能一律解释为关系边。经 386 份 N400 SHA 全量验证后，存在两种已定界
编码：普通节点仍为 `<uint32 ref, uint16 relation>`；而 type `1`、`node_id=2/3 且 type>=2`、以及
type `170+` 为 `<reserved:uint16, relation:uint16, child_ref:uint16>`。后者的节点头第 4 个 uint16
是 `parent_ref`。已确认关系码仅为 `181/182/183/184/190/201`，且必须同时满足 `reserved=0` 才输出边。

按正式解析器的布局优先级（171 份计数布局、63 份前置表布局；不把 152 份完整混合布局重复计入）汇总，
三元组形式已恢复的关系项包括：type-1 为 `184,593` 条、`node_id=2/3,type>=2` 的低类型关系表为
`30,019` 条、type `170+` 为 `39,804` 条。type-1 的 `234` 个零关系扩展、type-170 的 `226` 个
非零零关系扩展，另以原始本地引用清单保存；它们不是普通边。`node_id=3,type=0` 的 `1,407` 个非零值
则是零关系锚点列表，已单独输出。它们都不能用于 UCI、图层或
SVG 关联，更不能自动命名为构件或可见图元。

零关系控制项现已可按目标命名空间进一步说明。`node_id=2,type=1` 的 234 个非零扩展中，233 个
精确指向 PSM 命名对象 `Level 5`，1 个特殊导出指向 `Level 42`；`node_id=3,type=170` 的 226 个
非零扩展全部指向命名对象 `Default`，且每条还带一个零目标伴随项。这些是层级/默认状态引用，不能当作
管件、焊口或可见图元。`node_id=3,type=0` 锚点列表出现在 234 份计数型 SHA：231 份各有 6 个固定模板
锚点，分别解析到右侧面板分隔线、左右/上下边框及页面外框包络；其余 3 份各多 1 个锚点，精确等于
`Sheet5222`、`Sheet4779` 或 `Sheet6053` 的本地起始编号。因此第 7 项是后续物理页内容根，不是同线
管道的分图点、构件或坐标。

对计数与前置表两种布局的 `181/182/183/184/190/201` 关系码按 234 份 SHA 全量目标分类后，
可给出以下有限但稳定的“路由”含义：`181` 的 `20,381` 条中 `19,197` 条目标为 JSite 资源；
`182` 的 `22,719` 条中 `19,899` 条目标为 Sheet 本地根；`183` 的 `42,426` 条中 `39,589` 条目标为
PSM 命名图层/层级对象；`190` 的 `127,366` 条中 `14,524` 条命中有界 `0x0089` 动态属性，`5,847` 条
命中动态 graphic reference；`201` 的 `42,918` 条中 `2,621` 条命中动态 graphic reference。`184` 只有
`1,659` 条，混合命中 JSite、命名对象和 Sheet 根，尚不能命名为单一业务关系。

计数型/带前置表的 `0x0000` 布局中，`201` 还获得了更严格的页面交叉证据：先筛选为 UCI 动态 graphic id 后，
全库 `5,863` 个不同目标现已可在同一 SHA 的已解码 Sheet 记录中全部命中：除直线、圆/圆伴随、圆弧和
`0x007B` 复合对象外，最后 5 个是 E/N/EL 三行坐标注释的 `0x004D secondary_ref` 文字组。
这里的 `201` 因而是**动态 graphic 到 Sheet 记录的路由**；它不是构件类别，
也不证明一个 UCI 必定只对应一个可见几何。它与长前缀布局中 `201 -> 0x0013` 几何伴随组的含义不同，必须按
具体空间表布局分别处理，不能做全局替换。

最稳定的页面模板主路由是 `parent=238`：`181 -> 0x022F(559)` 共 `19,197` 次、
`182 -> 0x018E(398)` 共 `19,662` 次；同时 `183` 把该父节点连接到完整的命名图层集合。这证明这些关系是
页面/模板对象图的连接，不是管件拓扑。所有关系码仍只可作为命名空间路由证据，不能仅靠关系码新增 SVG
线、法兰、焊缝或文字。

`PSMspacemap/0x00008000` 的导出变体也新增一类完整边界：386 份中 69 份为原有完整节点表、315 份没有
该流，另有 2 份为“4 个 uint16 头字段 + 前置索引 + 普通 `<4H> + <IH>` 节点表”。
`N400P3A-AMSS1-N423201-01-0.sha` 含 8 个前置索引和 1,501 个节点；
`N400P3C-SWS4-N481001-01-0.sha` 含 93 个前置索引和 2,271 个节点，并在节点间有四段长度为
`12/6/12/6` 的全零填充。两者均已精确消费到流尾。全零区是分隔填充，不能误作 `id=0,type=0` 节点；
同样不能把前置索引当作图元或坐标。

`PSMsegmenttable` 的载荷也已从“未知字节”提升为确定的段存在索引：全量 386 份中，每个非零载荷槽都对应
同编号 `PSMspacemap/0x{段号×0x2000}` 流，每个零槽均无该流，零例外。因而该表可用于发现 SHA 中实际存在
哪些空间表段，但载荷值 `1` 与 `9` 的细分业务含义尚无跨样本直接证据，不能命名为管件类别、图层或可见性。

`PSMroots` 中此前未落点的 `TopVFSet -> 0x10BB(4283)` 现已定位：全库每份均在 `PSMcluster0` 有且仅有
一条 `0x0067 + uint32(20) + 5 个 uint32` 的固定短记录，其 `object_ref@6` 正是 `0x10BB`，其余四个值固定为
`(0,0,3,0)`。这证明它是 PSM 顶层集合对象的根链接，并非缺失流或未解析 Sheet；但常数 `3` 仍只是
`role_raw`，不能猜成页面、图层、构件或绘制命令。

根目录命名为 `Dynamic Attributes Set Table` 的 `0x2000` 前置序列仍不能直接还原为属性记录列表：386 份中它与
有界 `0x0089` 动态属性 reference 的交集恒为 `0`。它会与可见 Sheet 编号（共 3,877 次、344 份）、
PSM 包络编号（982 次、234 份）、命名对象编号（143 次、105 份）及动态图形编号（109 次、65 份）发生
不稳定的纯数值交集；没有任何记录边、父子关系或固定一对一规则把这些交集提升为绑定。因此保留为“根目录
归属的内部索引序列”，不可用来定位文字、法兰、焊缝或 UCI。

`PSMroots` 的根目录在 386 份 SHA 中完全一致：`DocStore -> 0x0000`、
`Dynamic Attributes Set Table -> 0x2000`、`_SupportOnlyList -> 0x4000` 均有对应空间流；
`TopVFSet -> 0x10BB` 与 `StyleLibrarian -> 0x0001` 不对应空间流。该目录因此是导出对象空间的根路由表，
不是图元清单；根名只能解释流的功能归属，不能直接赋予其中 uint16 值以构件或页面含义。

进一步的目录交叉验证已确认根引用并不处于单一编号空间：`DocStore -> 0x0000` 同时对应
`PSMclustertable` 的目录项 `0: PSMcluster0`，`StyleLibrarian -> 0x0001` 对应目录项
`1: StyleCluster`；二者不是图元、UCI、页面或空间表对象编号。相对地，`TopVFSet -> 0x10BB`
对应有界 `0x0067` 顶层集合对象记录，`0x2000/0x4000` 则对应 `PSMspacemap` 流。解析器必须先按
目录、对象记录和空间表三种路由分别消解，不能仅按数值猜测含义。

`0x007B` 复合记录的子项现增加一条全量验证的图形回链：386 份 SHA 的 1,032 个物理 Sheet 中，
全部 `449,504` 个非范围子项可在同一 Sheet 精确解析为 `18/32` 线（`445,922`）、`61` 圆弧（`782`）
或 `59/2B` 圆/椭圆（`2,800`）；
每一条的目标 `graphic_ref` 都与复合父对象号完全相等。因此复合记录是已绘制线/弧的组合与边界描述，
不是另一份应重复出图的独立几何。`type=6` 同时可回链到线、圆弧和圆/椭圆，不能命名为图元或管件类别；
`type=0` 是局部范围项（`43,468` 条）。

`type=0` 的局部范围角色已完成全量量化：`43,468` 个范围项中，`38,381` 个与其直接非范围子项的外包络
完全一致；直接成员数为 3 的有 `35,590` 个、为 1 的有 `2,785` 个、为 4 的有 6 个。其余 `5,087` 个
不包含直接可见子项，应视为逻辑/版式范围。全库未发现一个 `type=0` 范围直接包含另一个 `type=0` 范围。
因此它可用于构件或 UCI 的局部边界与组合归属，但不能输出为线、圆弧或独立构件。

StyleCluster 的固定 `0x002A` 记录也完成了全量交叉验证：每份 SHA 均固定有 style ref `3、5、12、16`
四条，且在全部 1,032 个物理 Sheet 已解码的 `18/32` 线、`4D` 文字、`59/2B` 圆/椭圆、`61` 圆弧和
`13/63` 圆伴随图元中直接使用次数均为零。其唯一目录回链名称为 `Normal`、`ANSI`，所以它们应视为
内部默认/兼容样式资源；`opaque_color_or_flags` 仍只保留原值，不能擅自解释为 RGB。与此同时，`0x007C`
本地资源组的每一个成员均已分类为本地线、弧、多边形、圆/椭圆或文字模板，局部符号库组成关系已经闭合；
但尚无这些资源直接放置到物理 Sheet 的记录证据。

对 `PSMspacemap/0x0000` 计数节点中未进入常规关系码 `181/182/183/184/190/201` 的六字节载荷进行了
全量审计：它们仅出现为已定界的零关系控制项、零关系局部引用或布局锚点。不存在另一种具有稳定边界、
并能与 Sheet 图元 reference 对应的隐藏线、圆、文字、法兰或管件记录族。因此当前 PSM 的剩余未知项属于
对象组织、模板/布局锚点或内部控制语义，不应再假设为“漏解析的可见构件”。

此前无 `CONTENTS` 位图载荷的 `JSite559` 现已解码为模板路由，而不是空对象：386 份 SHA 中其
`JProperties` 均为有界 `OLES` 属性，属性码 `3`、UTF-16 值恒为 `"221"`；它的 `646` 条 `0x003D`
放置记录全部位于非 `Sheet221` 的物理 Sheet，且同一文档均存在 `Sheet221`。因此解析器将其明确输出为
`contentless_jsite_template_sheet_stream = Sheet221`：物理页应叠加 Sheet221 的图框、标题栏和修订模板图元，
而不是把 JSite559 当作位图、管件或不可解释占位符。

`JSite690` 与 `JSite1402` 则是具有 DIB 位图内容的图框资源。其位置、尺寸不能再通过在 Sheet221 原始字节中
搜索资源号来猜测；现已统一使用 Sheet221 中有界 `0x003D` 放置记录的 `jsite_resource_id`、
`placement_origin`、`placement_size` 字段定位，并保留其 `primitive_ref` 以追溯。这样两张图框位图的
坐标、缩放和资源关系均直接来自 SHA。

`PSMclustertable` 也在全量 386 份中精确消费到流尾，所有目录名均有同名 OLE 流。`PSMcluster0` 的 child
目录仅有四种固定组合：仅自身 `315` 份、`自身+Sheet6` 为 `61` 份、再加 `Sheet12` 为 `8` 份、再加
`Sheet39` 为 `2` 份；其他 Sheet 条目均没有 child。它最多是流包含/早期物理页登记，不能表示某个
PSM 包络属于该页，更不能作为构件、层或可见性的依据。

固定编号的 `Sheet12`、`Sheet39`、`Sheet65`、`Sheet91`、`Sheet117` 已在全部 386 份 SHA 中逐个核验：
每个流都严格等于 8 字节 `44f5906c00000000`（小端为 `0x6C90F544 + uint32(0)`），没有页头、文字、图元或
坐标。这五个流是已注册的空 Sheet 占位，不能计为物理 ISO 页，不能用于判断同线分图、图纸容量或分图点，
也不能从中恢复模板或图形。解析报告将其显式标为 `fixed-empty-sheet-registration-stub`，若将来出现字节差异
则保留为未验证流而不套用本结论。

标准 OLE 的 `\x05SummaryInformation` 与 `\x05DocumentSummaryInformation` 已作为文档溯源属性完成解码。
386 份中稳定值为：模板 `PANDA3_IFC.Sha`、标题/关键词 `D Wide Border`、类别 `Border`、创建程序
`Shape2DServer Application`、注释 `Contains Title, Signature, Issue, and Revision areas.`、代码页 `1252`。
作者、最后保存者和修订号会随文件变化（作者以 `lmeng1` 为主，修订号 `3/4/5`）；创建/保存时间也可读取。
这些字段仅说明 SHA 的模板、导出应用和文档版本来源，不是 Shape2D 图纸内容，绝不可用于恢复图元、字体、
二维坐标、UCI、焊缝或分图点。

`0x0000` 混合关系序列中的 `relation=183` 现可作受限的“容器到命名图层成员”解释。152 份完整混合布局
样本中，共 `43,484` 条该关系；其父引用从不直接等于命名图层对象，而目标会重复精确命中 `PIPE`、`FITTINGS`、
`WELDS`、`DIMLINES`、`MATLIST`、`FRAME`、`Border`、`Level n` 等 `0x0081` 命名层。每个父容器的目标集合
与同文件的基础 175 项或后续物理页 92 项命名层组高度重合，因此可在报告中输出 `target_layer_names` 作为
PSM 层级成员清单。它不代表该图层在当前页可见、也不等价于页面图元清单；父容器不是管件、焊缝、坐标或
可绘制 Shape2D primitive。

`relation=184` 经同一批 152 份完整混合布局核对后仍是明确的多态路由，不能赋予单一业务名称：最常见的
父容器会分别指向命名层 `01`、`Level 27`、`Default`，`parent=238` 时指向 JSite `559`，少数情况下再指向
物理 Sheet 根或 PSM 段内地址。解析报告因此按 `parent_ref` 输出 `relation_184_parent_contexts`，而不是只给
总数或误称为法兰/图层/页面关系。任何渲染、分图或构件判断都必须继续依赖独立的 Sheet/PSM 直接记录。

这 23 个混合/歧义样本的单 child 尾根仍可保留为独立证据：14 条 `relation=190` 目标精确命中
`Unclustered Dynamic Attributes` 的有界 `0x0089` 属性引用；9 条 `relation=201` 不命中已验证节点。
后者中 8 条为 `0x8000/0x8001/0x8002`，落在现存 `PSMspacemap/0x00008000` 段内偏移 `0/1/2`，另 1 条
`0x1FFF` 落在 `0x00000000` 段末端。这只能证明其为段内地址/选择器，不是节点、坐标或可见图元；
偏移的业务语义仍未解码，不能生成 SVG 几何。

`StyleCluster` 的文字字体链也已完成。`0x002C` 字体定义记录满足严格长度公式
`record_length = 70 + 2×字体名字符数`：文字样式定义在 `+20`，字号比例 double 在 `+48`，
字体名长度在 `+74`、UTF-16 字符串从 `+76` 开始。固定 90 字节的 `0x002D` 则把实际 Sheet
文字样式 `+20` 连接到字体定义样式 `+44`。十份样本的全部 `6,350` 条有界 `0x004D` 文字记录
均可通过此链路解析字体和字号比例，零缺失；例如 `0x00F4 -> 0x00E3 -> Courier New / 0.005`，
`0x00FB -> 0x00FA -> Arial / 0.0028`，中文标题 `0x00D5 -> SimHei / 0.004`。该链路应决定
字体族和名义字号；最终字宽/字高仍优先使用 PSM 字形包络，避免因 SVG 字体度量差异引入偏移。

另外三类非图元流已完成架构归类。`JSitesList` 的精确格式为 `OLEM + uint32 数量 + 数量个
uint32 resource id`，十份样本恒为 `559/690/1402`；其中 `690/1402` 是两张已嵌入图框位图，
可从自描述 BMP 头直接读出为 32-bit 的 `866×498` 与 `537×212` 像素；
`559` 仅有 JProperties、没有 CONTENTS，仍应标记为外部/未解站点。其 18 字节 JProperties 已可精确定界为
`OLES + uint16(0) + property_code=3 + UTF-16 code-unit-count=4 + "221\\0"`；386 份 SHA 完全一致。
`221` 只是外部站点的候选标识，不能仅因数值相同就断言它绑定 Sheet221，也不能据此补造图片或几何。
`AppObject` 的有界 UTF-16

此前只在 Sheet221 中识别的 234 字节 `0x003D` OLE 放置 wrapper 也存在于物理 Sheet：全库 386 份
SHA 共 `646` 条，`+162` 的 JSite 引用全部为无 CONTENTS 的 `559`。每份文件的 Sheet6 开头都稳定为
`primitive_ref=398, sheet_ref=6, page_layer_ref=8, JSite=559`。因此它是页面级外部 OLE 放置关系，
不是管件、UCI、焊口或可直接绘制的位图；wrapper 内其他变换字段仍需独立证据才能命名。
provider path 为 `r:\rad2d\bin\igrSmartLabel.dll`，表示 SmartLabel 插件依赖。`DocVersion3`
则由 NUL 分隔的 `模块/版本/mode/时间` 四元组构成，是 Shape2DServer 版本历史（十份通常 4 条，
AMSS1 为 5 条）。这三类均属于导出环境与资源元数据，绝不能作为 SVG 坐标、文字或图元来源。
即使 resource id 的四字节在某个 Sheet 内出现，也可能只是坐标/编号碰撞，不能据此认定 JSite
绑定或尝试把无 CONTENTS 的 `559` 绘制出来。

JSite 的 OLE 包装也已核对：`690/1402` 都有相同的 106 字节 `\x01CompObj`，其自描述字符串为
`Picture (Device Independent Bitmap)` 与 `StaticDib`，并有 20 字节固定 `\x01Ole` 头；这与其
`CONTENTS` 中的 32-bit BMP 完全一致。`559` 仅有 `JProperties` 的 `OLES` 标记，没有 CompObj、Ole
或 Contents。因此 `690/1402` 可作为图框嵌入位图资源处理，而 `559` 只能作为无内容的外部 OLE 占位。

`TaggedTxtData/*` 的 11 个流也已全部按 UTF-8 XML 结构化。十份样本中 `Sheet221` 的 Revision
绑定表达式稳定只引用 6 个字段：`MajorRev_ForRevise`、`RevisedBy`、`RevisedDate`、`CheckedBy`、
`ApprovedBy`、`RevisionDescription`。因此这些字段在存在对应绑定时可从 `TaggedTxtData/Revision`
取显示值；例如修订说明可显示为 `ISSUE FOR CONSTRUCTION`。`TitleArea`、`SignatureArea`、
`Configuration` 等流也保存字段值（如 DrawingNumber、PlantName、创建/修改时间），但当前样本
没有其直接 Sheet221 绑定证据，不能用 XML 自行创造图框文字、坐标、字体大小、引线或几何。

根引用也可能是节点 id 而不是流地址。已完全解析的空间表中，部分样本的
`TopVFSet`、`StyleLibrarian` 或 `DocStore` 可命中 type-3 节点，并以关系
`190` 为主、`201` 为少数；命中流可为 `0x8000`、`0xA000` 或 `0xC000`。
其他样本没有已验证命中，因此此路径只能逐文件记录，不能推广为固定表或
自动对象分类规则。

这些已命中根节点的 15 条 `190/201` 子边需要区分两种动态属性引用：12 条精确
命中 `Unclustered Dynamic Attributes` 中通用的
`0x0089 + 长度 + uint32 引用`字段；该字段既出现在 `_ISO`，也出现在
`Element Tag` 等属性之后。其中 9 条是 `_ISO` 的紧凑 30 字节记录，3 条是
`Element Tag`（149/234/240 字节）记录。它们仍全部不命中 UCI 专用的
`dynamic_graphics()`集合、`PSMcluster0` 包络或已完整解析空间表节点。故这 12 条可
确定通向动态图纸/属性对象，但不能直接等同于管件 UCI 或 Sheet 图元。剩余 3 条也不再是未知变体：
`0xC001` 精确指向现存 `PSMspacemap/0xC000` 的基址加 `1`，两条 `0x8002`
精确指向现存 `PSMspacemap/0x8000` 的基址加 `2`。这证明它们是空间表基址选择器；
偏移 `1/2` 的具体业务语义尚未解码，不能擅自命名为页面、图层或可见对象。

另有两个 RHO1 样本只含 `0x0000/0x2000/0x4000/0x6000`，缺少 `0x8000`。
这是已确认的精简导出变体，不是 SHA 损坏；解析器应继续读取现存表，并明确
报告 `0x8000` 缺失。

流级全量盘点还完成了两条固定注册流的归类。`Dynamic Attributes Metadata` 在 386 份中均为 28 字节：
签名 `0x6C90F544`、格式版本 `1`、标志 `0x40000000`、末尾三个零 uint32；它是动态属性空间的注册/版本头。
`JTaggedTxtStgList` 在 386 份中均为 70 字节，注册 `TaggedTxtStorages` 与 `TaggedTxtData` 两个名称；
它只说明后续 XML 标注数据的存储位置。两者都不是 UCI、构件属性、Sheet 图元、坐标或 SVG 几何来源。

`DocVersion2` 也在 386 份中保持完全相同的 129 字节（SHA-256
`d0e8a9ace5eeece8161a3e6998c92b9cd424f12af0657f011ac1720b98f30019`）。目前只能把它归为固定的
遗留文档版本/兼容性档案；其中的整数尚无独立记录边界，不得解释为 PSM 节点、引用、坐标、构件或绘图命令。

对 `0x2000` 的原始 Sheet221 交叉核对推翻了“模板页对象选择索引”的临时推断：十份样本中其值与已解码 Sheet221 文字、线段、矩形、椭圆和复合可见图元引用均为零交集；uint32 原始字节命中也全部为零，uint16 裸字节命中最多仅 3 个，不能构成记录关联。因此它是根目录已命名的 `Dynamic Attributes Set Table` 的内部非重复索引序列，不是 Sheet221 图元，也不是全局 graphic id。

全量 386 份的顺序复核进一步排除了“统一排序空闲 ID 表”的解释：全部 `9,166` 个 `0x2000` 值在各自文件内均不重复，但只有 `231/386` 份严格递增；其余文件合计有 `1,448` 次回跳和 `3,474` 次大于 1 的正跳。解析报告因此保留顺序统计，但不能假设该表的顺序代表编号分配、绘制顺序、坐标次序或图元层级。

中段进一步确认存在第二种关系边布局：`<记录类型, 子项数, 重复子项数, 父号> + N 个 <保留=0, 关系码, 子号>`。其中 type-2 是单边容器，type-3 可批量装入边；其父号会延续前缀中的 child 引用，说明它们是 PSM 内部层级而非 Sheet 图元。9 份常规样本中恰好各有一个固定 20 字节的 `child_ref=0` 扩展变体；跳过该变体后普通容器继续成立，可恢复 890--1532 个容器、1149--1838 条边，直到后续零子项控制块才停止。AMSS1 是独立的前置变体，尚未纳入此规则。

后续零子项控制块也已找到边界：`type=3, child_count=0, repeated_count=N, parent=0`，其后为 N 个 `<保留=0, relation=0, child_ref>` 三元组。将普通容器、固定零目标变体和 7 个零关系列表合并后，9 份常规 SHA 的完整中段均能恰好解析到尾根块之前，无剩余字节。零关系列表中的 child_ref 仍未确定属于何种对象命名空间，不能用于几何还原。

对该完整中段的关系目标补做分段验证后，十份高复杂 SHA 的每一条已定界关系目标均满足
`base = target & 0xFFFFE000` 指向本文件实际存在的 `PSMspacemap/base`。`183/184` 固定落在
`0x0000` 段；`190` 落在 `0x0000` 或 `0x6000`；`201` 落在 `0x0000`；`181/182` 可少量跨到
`0x8000/A000/C000` 或 `0x6000`。这证明其底层编码是段内地址，而非统一的节点 ID 或 Sheet 图元 ID。
同一数值有时会“碰巧”同时落入动态属性、命名对象或 Sheet 本地编号范围，报告会并列保留这些交集，
但没有已验证记录边时不能把数值交集升级为构件、图层或几何绑定。

混合 `0x0000` 末端的 7 个零关系列表也已完成布局级归类。十份高复杂 SHA 中，其非零引用顺序稳定为
`3242/3845/3870/4367/4396/4451/最后物理 Sheet 根`：前五个都具有极细的 PSM 包络，分别构成共享
材料表/图框区域的上、左、横、下、右边界；`4451` 是覆盖页面范围的布局容器；最后一个值随文件变化，
但逐份精确等于最后一张物理 Sheet 的本地根 ID（例如 `Sheet6788 -> 6792`）。因此该组是模板框架与
页面锚点，不是管道、法兰、焊缝或需要新增绘制的 SVG 图元。

`PSMcluster0` 的连续 `<uint32 ref + 5×uint16>` 包络记录现也采用“三条相邻记录以上、页内范围、
小标签值”的保守规则定界，而不再只靠裸字节搜索。DMW 样本得到 23 个连续段、4,523 条记录，尾部标签
仅为 `0/2/5/6/9/11/17`。交叉已定界 Sheet 图元后，`11` 精确覆盖上述五条模板框边，页面布局容器为
`5`；`6` 与椭圆记录高度相关（23 个命中中 21 个）；但直线和复合对象同时分布于多个标签。因此当前
只能称该字段为 PSM 内部记录子类型标签，不能直接命名为管件类别、图层、可见性或绘制顺序。

两个罕见单例标签也已定位：十份复杂 SHA 中 `tag=17/ref=3067` 与 `tag=9/ref=3256` 的包络坐标固定，
分别为 `(7505,9389)-(7751,9390)` 和 `(7505,9451)-(7751,9452)`。二者都精确对应 `Sheet221` 的
`18/32` 模板直线（`graphic_ref=221`、`page_layer_ref=332`，命名层为 `01`），因此可标为固定模板
分隔线子类型；不是随管线变化的尺寸线、法兰、焊缝或管道图元。

扩大到十份复杂 SHA 后该结论保持稳定并补充了圆弧证据：有 `188/211` 个已定界椭圆命中连续包络段，
其中 `179` 个标签为 `6`；全部 `21/21` 条 `0x0061` 管道圆弧都命中标签 `11`；`7B` 复合对象则以
`5` 为主（`7,154/9,982`）。但是 `18/32` 线与 `7B` 复合对象仍可分布在 `0/2/5/6/11/16/21` 多个值，
所以这些是已验证的相关性而非一一类型定义。尤其 `11` 不能只称为“图框边”，它同时用于圆弧相关记录。

标签 `0` 现已取得更强的单一记录族证据：十份复杂 SHA 中，`0x004D` 文字记录的 `child_ref@6` 有
`7,380` 条进入可信连续包络段，且 `7,380/7,380` 的尾部标签都为 `0`。因此可将 `0` 限定标记为
“文字包络记录子类型”；该结论不涉及字体名、字号、图层、显示开关或文字绘制顺序，后者仍由 Sheet/
StyleCluster 的独立字段决定。

对具有直接 `page_layer_ref@14` 证据的 `18/32` 线，再按图层交叉十份复杂 SHA：标签 `2` 的
`33,778` 条可信包络线全部属于 `PIPE`，没有任何 `FITTINGS`、`DIMLINES` 或 `ISOTEXT` 例外。
反方向不成立，`PIPE` 还会出现 `5/6/11/16/21`；因此报告只把 `2 -> PIPE` 保存为当前样本的
单向层关联，不把它提升为“所有管线”或“管径/材质/线型”的定义。

上述 PSM 标签规律已扩大到全部 386 份 SHA，并且没有解析失败：`0x004D` 文字的
`101,509/101,509` 个可信包络仍全部为 tag `0`；`0x0059/0x002B` 椭圆的 `5,370/5,416` 个匹配
为 tag `6`；`0x0061` 圆弧和 `0x007B` 复合对象则分布在多个 tag。因此 tag 仍只能作为
“PSM 内部包络子类型”的溯源字段，不能直接命名为焊缝、法兰、阀门、线宽或可见性。

进一步的全量交叉已经可以**排除两种错误解释**：该包络末尾 uint16 tag 既不是 Shape2D 图元类型，也不是
页面图层编号。tag `5` 同时命中直线、复合、圆形，并跨 `DIMLINES/FITTINGS/ISOTEXT/PIPE`；tag `6` 同时命中
直线、复合、圆形、圆弧，并跨 `DIMLINES/FITTINGS/PIPE/WELDS`。因此它只能保留为 PSM 内部子类追溯字段，
不可作为法兰、阀门、焊口、图层、可见性或 SVG 绘制规则。

`StyleCluster` 中的 `LW3C1`、`LW6.5C5` 等 UTF-16 名称也已完成真正的条目定界：固定 30 字节头
（6 个 uint32、1 个 uint16、名称字符数 uint32）后紧跟恰好对应字符数的 UTF-16 名称。所谓 `0x0043`
实际就是字符串中字母 `C` 的 UTF-16LE 字节，不是二进制记录标记。扩大到全部 386 份 SHA 后，全部
`3,131/3,131` 个目录条目的 `object_ref` 都精确命中一个有界 `0x002E` 线样式对象，因此报告可安全输出
`LW 目录名 -> 线样式 ref` 链路。`LW3.5`、`C5`、`P1001` 的工程含义仍未验证，不能仅按名称换算实际
SVG 线宽、颜色、图层或构件类别。相同目录格式还包含 `Dash`：全库 `763/763` 个 `Dash` 条目的
`object_ref` 精确命中固定 66 字节的 `0x002F` 虚线模式记录（样式 ref 仅见 9、15、230）。这已证明
`Dash -> 虚线样式对象` 的索引链，但 `0x002F` 其余 payload 尚未解为 SVG dash-array，不能凭空补出
虚线节距。其结构已能原样读取为 `scale_ratio@40`、`segment_count@54`、两个 double 比例
`@56/@64`；全库三种组合固定为 `9:(0.5,2,0,-1)`、`15:(1,2,0.00035,-0.00175)`、
`230:(0.5,2,-0.0035,-0.00175)`，其中 `230` 是 `Dash` 目录所指对象。全库再按已验证的
`0x0018/0x0032` 线、`0x0059` 圆、`0x0061` 圆弧及 `0x0013/0x0063` 圆伴随记录交叉后，只有
`230 -> 231` 出现：每份恰为 `Sheet221`、layer `01` 的一条固定短分隔线；`9 -> 11` 与
`15 -> 18` 在这些已解码图元中均为零次。负号和 scale 的绘制含义
尚未被独立证明，且三种 style ref 不直接出现在当前已解码的普通 Sheet 直线或后续页 `18/32` 直线中，
所以禁止将它们直接写成 SVG `stroke-dasharray`。

对 StyleCluster 的局部资源是否被当前 ISO 页直接实例化也已做了全库反向交叉：386 份 SHA 中的
`3,860` 个局部多边形（`0x0084`）、`2,702` 个局部圆弧（`0x0061`）、`386` 个局部圆（`0x0059`）、
`8,878` 个局部线（`0x0018`）、`1,930` 个局部文字模板（`0x0070`）和 `3,474` 个组合组（`0x007C`），
合计 `20,730` 个 object ref，在所有已定界物理 Sheet 的线/文字/圆弧/圆记录引用、graphic ref、
page-layer/style ref、composite ref 与 composite child ref 中均为零命中。它们因此是样式库/局部定义，
不是可按 object ref 直接画到当前 ISO 页上的法兰、仪表或其他构件实例。此结论仅限已解码的引用槽位；
若未来找到独立的实例记录族，才可重新建立实例链。

`0x002E` 的 58 字节变体也已完成与该模式表的直接闭合：全库 `1,158/1,158` 条尾部 uint32
都精确是一个 `0x002F` 模式样式引用，三条固定关系为 `11 -> 9`、`18 -> 15`、`231 -> 230`。
因此该变体的尾值不是 RGB；`Sheet221` 固定分隔线的 style `231` 通过它指向 `Dash` 模式。该链仅证明
样式层级引用，不替代前述对 `0x002F` 负号、缩放和 SVG dash-array 公式的限制。

`StyleCluster 0x002A` 也完成了直接使用范围核验：386 份文件固定都有 style `3,5,12,16`，其中
`3 -> Normal`、`5 -> ANSI` 有目录名，而这四个 style 在已解码线、文字、圆、圆弧图元里全为零次。
因此它更接近默认/目录资源，不能把 `@34` 的原始 uint32 或 `@44=1.0` 直接说成可见颜色、线宽或
SVG 参数；零次仅限已解码图元，不排除未知记录族的内部使用。

`StyleCluster 0x002E` 中另一个已验证字段是 `double@40` 的 `line_width_ratio`，它与同一记录的
`style_ref@20` 一起构成直接的 Sheet 线样式登记链，例如 style 1 为 `0.00035`、style 229 为
`0.001`。这两个字段可用于已验证的 SVG 笔宽换算。该记录还已完成结构拆分：
`category_raw@18`（全库仅 1、2）、`flags_raw@32`、`auxiliary_u32_raw@34`、
`line_width_ratio@40` 与尾部 uint32；这三个 `raw` 字段的记录边界和取值稳定，但尚未能独立证明其工程
含义，不能把 category 叫做管线类别、把 flags 叫做端帽/填充，也不能把该辅助槽位当成页面对象引用。
58 字节变体的尾部 dash 引用、填充/端帽等仍不能由这些字段推断。

对 `category_raw` 也补充了完整物理 Sheet 反向使用核验：在 `459` 份 SHA 中，已解码的直线、圆、圆弧和
`0x004D` 文字实际使用的 `0x002E` 样式均为 category `1`；category `2` 仅有 styles `13`、`14`、`42` 三个登记项，
在上述图元族中均为零次。此结果只排除“它是当前已解码页面图元的直接样式”，不能据此把 category `2`
命名为填充、隐藏、全局未使用或任何构件类别。

`Unclustered Dynamic Attributes` 中有两种同结构的 `PipeLine Info` 记录，不能混为一类。全库 `459` 份 SHA 的
`49,448` 条有界记录中，`32,007` 条同时具有非空的 `PipeLine Reference`、`Fly Text` 与
`Unique Component Identifier`，可定义为构件实例；`17,441` 条这三项均为空，是属性 schema 占位记录。
构件实例仍可有空的 `Element Tag`（`5,235` 条）或根本没有该属性（`15` 条），因此 Element Tag 不能反向作为
“是不是构件”的判据。schema 占位记录的 graphic ref 不得参与 UCI、构件覆盖率或 SVG 构件归属统计。

PCF 已验证为 `WELD` 的 `000138AA` UCI 也不能简单等同于单个黑点：它经 SHA 的直接 graphic ref 交叉后，可命中
圆/圆伴随记录、直线、复合对象及这些对象的组合。因此焊缝 UCI 表示一个 SHA 图元组；给焊缝生成菱形标记时，锚点
必须通过该 UCI 的已验证成员和几何关系选取，不能仅按图面上距离最近的黑点匹配。

将同目录 PCF 的 `COMPONENT-IDENTIFIER/UCI` 进行交叉后，GUID 第一段可作为**受限的对象族线索**，但不是完整规格：
`000138AA` 的 `9,090/9,090` 个匹配 PCF 行都是 `WELD`；`0001388C` 的 `4,934/4,937` 为 `PIPE` 或
`PIPE-FIXED`；`000138B6` 的 `264/266` 为仪表族；`000138A9` 的 `2,883/2,883` 为 `BOLT`；`000138A8` 的
`2,381/2,381` 为 `GASKET`。反例同样重要：`00013885` 同时覆盖法兰、弯头、阀门、三通和变径，`000138B7`
也跨越多种杂项构件。因此前缀只能帮助 PCF 审计时缩小候选构件族，不能替代完整 UCI 精确匹配、不能决定 SVG 符号、坐标或 SHA 图元归属。

`0x003D` 的 234 字节 OLE/JSite 放置记录也进一步完成了**内部自校验**。全库 `1,418/1,418` 条均满足：
放置起点 `<double x,y>@42` 会在 `@122/@130` 原样复写，宽度 `@82` 会在 `@138` 复写，`@154` 恒等于
`height@90 / width@82`，`@170/@178/@186/@194` 恒为单位二维矩阵 `(1,0,0,1)`，而 `@66` 的原始缩放值会在
`@218` 再次出现。故可安全还原 JSite/图框位图的放置框与长宽比；但 `@66` 缩放值、`@202/@210` 尾部起点
及未来可能出现的非单位矩阵仍不能擅自命名或解释为管道构件坐标。

对其中带 `CONTENTS` 的 BMP-DIB 资源，`@66` 也已不再是未知缩放值。BMP 文件头的 DIB 部分提供
`x/y pixels-per-metre`（BMP 文件内偏移 `38/42`）；原生米制尺寸为 `pixel_width/xppm`、`pixel_height/yppm`。
全库 `772/772` 个嵌入位图放置记录均满足 `原生米制尺寸 × 0x003D@66 = @82/@90 放置宽高`，相对误差不超过
`0.01%`（SHA 浮点坐标与 DIB 整数密度的导出舍入）。因此 `@66` 对此记录族可确认是**位图物理缩放倍数**；
此结论不适用于没有内容流的 JSite559，也不能推成管道构件变换。

新增确认了一类**可复用的局部矢量模板**，但它不是图纸构件的页面坐标。`StyleCluster 0x0084`
有两种已验证 payload：88 字节时有 4 个点，104 字节时有 5 个点；均为 `object_ref@6`、局部样式/标志原始值 `@24`、
flags `@28` 后从 `@30` 开始的 `<double x,double y>` 点列，最后一点恒等于第一点，因而是闭合多边形。
`0x007C` 是通用成员组合记录，严格长度公式为 `16 + 4*child_count`；`object_ref@6`、`child_count@18`，
成员 ref 从 `@22` 开始。全库 386 份 N400 SHA 中恒有 9 个此类组：4 个双成员组覆盖 8 个五点多边形，
另有 2 个独立的四点多边形；还包括一个 7 成员组合（椭圆类资源、两个三角形、两个 `0x0018` 局部线段、两个
`0x0070` 子资源）。每一个
对象的字节内容全库一致。这已证明
“组合符号模板 -> 闭合局部轮廓”的结构，但尚未证明它在 Sheet 哪个位置被实例化，也没有工程语义
证据把它命名为法兰、箭头、焊缝或仪表。因此可保留对象引用和顶点，不能直接作为 ISO 可见图元绘制。

特别注意，`0x0084@24` 目前不能称为通用 `style_ref`：其值 `4/5` 同时会在不同 StyleCluster
命名空间中作为字体样式、固定样式或其他局部类型编号出现，尚无对象引用链证明它们相同。报告将该字段
保留为 `local_style_or_flags_raw`，禁止据此为多边形套用字体、线宽或颜色。

同一符号库中 `StyleCluster 0x0061,0x003B` 也已完成结构解码：payload 固定 59 字节，
`object_ref@6`、`@10..23` 固定零填充，随后 `@24` 起连续 5 个 double，依次为局部圆心 `x/y`、半径、
起始角、终止角。它与 Sheet 圆弧在几何字段形态上相同，但坐标属于 StyleCluster 的**局部符号空间**；
不能与物理 Sheet `0x0061` 坐标混用。9 个组合组中已可确认 7 条这样的局部圆弧被复用，仍没有证据说明
它们对应页面上的法兰、流向、仪表或任何具体管道构件。

`StyleCluster 0x0059,0x002B` 的局部资源变体现已完成几何解码：`object_ref@6`，`@10..23` 的
图形/图层/样式引用恒为零；`@24/@32` 为局部圆心 `x/y`，`@40` 为局部半径，`@48` 为末尾标志。
同长度物理 Sheet 圆形记录使用相同的圆心/半径/标志位置，因而字段含义得到独立验证；差异仅在于
局部符号坐标空间而非 Sheet 页面坐标。组 `8299` 的局部圆心约为 `(0,-1.27e-6)`、半径为
`0.00953495177`。组合关系则已完整可读：例如组 `8299` 有 7 个成员，分别是 1 个局部圆形资源、
2 个四点闭合三角形、2 个 `0x0018` 局部线段、2 个 `0x0070` 资源。这能证明符号库的组成，
不能证明它在 ISO 中的业务名称或落点。

此前暂称“控制值”的 `StyleCluster 0x0018` 现已确认是**局部两点线段资源**，应以本段为准：
payload 固定 50 字节，`object_ref@6` 后的 4 个 double（`@24/@32/@40/@48`）依次组成
`(x1,y1)`、`(x2,y2)`。全库 23 条都被 9 个 `0x007C` 组合组引用；其中一组 5 条线段闭合成矩形，
另一组 14 条形成局部轮廓，且两条线段端点与三角形顶点相接。这是充分的局部几何证据，但仍没有
“局部模板 -> 物理 Sheet 实例”的变换链，不能把这些点用于 ISO 页面坐标，也不能按形状命名业务构件。

`StyleCluster 0x0070` 亦不再是未知常量块。其 payload 公式严格为
`84 + 2*font_name_char_count@86`，UTF-16 字体名从 `@90` 开始；还可稳定读取局部锚点 double
`@18/@26`、scale `@34`、未命名的变换/旋转 raw 值 `@42`、字体大小比例 `@74`。全库 `1,930/1,930`
条都为 `Arial`，每个 SHA 恒有 5 条、对象 ref 恒为 `8222/8224/8252/8305/8306`，其中后两条属于组
`8299`。记录不含实际文字内容，也没有局部模板到物理 Sheet 的实例变换，因此只能还原其字体模板属性，
不能凭它生成某一页 ISO 的文字或坐标。

`StyleCluster 0x001B` 也完成了可靠归类。它固定为 202 字节的**命名内部样式资源**；`object_ref@6`
可在独立 UTF-16 目录中找到唯一反向名称。381 份文件各有 `Reference`、`border`、`Office Automation`
三条；另 5 份 S3D 文件额外有 `S3D_INTERNALSTYLE_UNLOCK_SCALE`、`LOCK_SCALE`、`LOCK` 三条。记录的
`category_raw@26`、`subtype_raw@30` 和 5 个 double 槽位 `@96/@108/@116/@124/@132` 已能稳定读取，
但尚无直接绘制证据，所以它们不是管道构件、页面坐标、图元或 SVG 公式，必须按样式模板元数据保留。

`StyleCluster 0x002A` 的目录关系也已闭合到可证明范围：它固定为 46 字节样式目录记录，
`object_ref=8195/style=3` 被独立目录命名为 `Normal`，`object_ref=8208/style=5` 命名为 `ANSI`；
style 12 和 16 没有目录名。`@34` 的 uint32 与 `@44` 的 double 均可稳定读取，但还没有实例渲染证据
证明前者是 RGB/颜色、后者是线宽或 SVG 比例，因此报告保留为 `opaque_color_or_flags` 和
`terminal_ratio`，禁止自行换算颜色或笔宽。

此前扫描中出现的大量 `tag=0x0000` 伪“新记录”现已归类为 **StyleCluster 对象库容器**：
`record_length@2`、对象号 `@6`（在本库以 `8192+` 分配）、原始类别 `@18`，嵌套 payload 从 `@30`
开始。父容器的 payload 会递归覆盖子对象字节，所以不能按每个零字节另起一条图元记录。全库基础对象
出现两套镜像库副本（多个稳定对象全库计数为 763），S3D 文件再引入额外对象号；这些是样式存储层级，
不是 ISO 的构件、图层或页面坐标。类别值仅保留为 `category_raw`，没有工程业务语义。

零关系列表的前六个非零编号现已确定为共享模板空间锚点：`0x0CAA`、`0x0F05`、`0x0F1E`、`0x110F`、`0x112C`、`0x1163` 在十份 SHA 的 `PSMcluster0` 中均对应相同的右侧面板分隔线、边框和页面级外框包络，不是管道构件。第七个编号随图纸变化，且十份均精确等于某个后续物理 `Sheet*` 的 header local-id start，例如 `0x1797 -> Sheet6035`；因此它是跨 Sheet 内容空间根引用，不是可见图元。

层级报告现会对每条 `0x0000` 关系边按可证明的命名空间归类。十样本中，关系 `182` 的 `861/957` 个目标精确命中物理 Sheet header local-id start，主要承担页面空间连接；关系 `190` 与 `PSMcluster0` 包络（`8035/11505`）及动态 graphic ref（`765/11505`）重合最多，主要通向 PSM/对象空间。这是路由倾向证据而非绝对重命名规则；`181/183/184/190/201` 仍有混合目标，输出时必须保留原关系码。

AMSS1 并非完整中段格式不同：从字节 `138` 开始可复用完整关系序列，恰好解析到尾根块，得到 `1886` 个容器和 `2193` 条关系边。其字节 `92..138` 已解为拆分存储的高层前缀延续：4 字节填充、节点 `7`（`4703/184`）、节点 `8`（`7/183`、`9/182`）和第二个零终止记录。它是 AMSS1 的存储变体，不能按常规关系容器解释。

## `0x004D` 文字锚点与 PSM 包围盒

新增 459 份 `/管道` SHA 验证：`123,964` 条物理 Sheet `0x004D` 文字中，`116,053` 条（`93.62%`）可经
文字自己的 `child_ref@6` 命中唯一的、连续记录边界验证过的 `PSMcluster0` 包围盒；没有任何已命中的文字
出现多个候选。`secondary_ref@10` 在同一页大量共享，因此是分组/关联引用，不能作为逐条文字的 PSM 查找键。

文字变换的 `x` 与 `y` 都使用 Shape2D 的 `16800` 归一化基准，即 `page_x=x*16800`、
`page_y=y*16800`；将 `y` 乘可见页面高度 `11880` 会造成整体纵向偏移。PSM 包围盒代表最终字形范围，
而 StyleCluster 的 `font_size_ratio` 只代表请求字号/样式比例，当前不能把它等同于最终字形高度。
无包围盒的其余 `7,911` 条文字应按同 Sheet、同样式的已验证文字回退排版，并在输出中标记为回退，
不能杜撰一个 PSM 范围。

## 混合 PSM 关系的目标命名空间

`PSMspacemap/0x00000000` 中的 `181/182/183/184/190/201` 不是图元类型，也不是单义的父子语义。
必须同时保留关系码、父命名空间和目标命名空间。特别是 `Sheet123` 流编号与 Sheet 头部 local-root 编号
是两个不同命名空间，数值可接近但不能互换。

对 `/管道` 的 `459` 份 SHA 中 `161` 份完整混合布局做同 SHA 精确交叉后，`190` 的目标已可拆分为
`101,602` 条连续 PSM 包围盒、`23,252` 条已解码 Sheet `graphic_ref`、`22,889` 条有界 `0x0089`
动态属性引用、`8,005` 条 tseg 节点；`201` 则有 `21,804` 条 Sheet `graphic_ref`、`6,266` 条
PSM 包围盒、`4,183` 条 tseg 节点。其余目标仍未命名。以上仅证明内部路由落入哪类对象空间；除已单独
验证的 UCI -> PSM -> Sheet 链路外，不能由关系码或命中类别直接命名法兰、阀门、焊缝或可见性。

进一步纳入 Sheet 的本地 primitive 引用空间后，上述“其余目标”绝大多数也已闭合：`190` 有 `129,916`
条、`201` 有 `46,666` 条精确命中已解码的本地引用，包括 `18/32 child_ref`、`0x004D child_ref`、
`0x0059/0x0061 primitive_ref` 或 `0x007B` composite child ref。它们是对既有 Sheet 图元的路由，不是
额外应绘制的线段或构件。最后两个 `201` 例外已逐条验证为 CW 图纸 `Sheet926` 中 `0x003D`、
`JSite559` 图框/模板资源放置记录的 `primitive_ref=0x0438`；因此本地引用集合补入 `0x003D primitive_ref`。
完整混合布局扫描现不再有未分类的 `190/201` 目标。所有这些边仍只表示“PSM 内部路由到已解码对象”，
不构成额外可绘制几何或构件身份。

后续对 `181/182/183/184` 的页级相邻编号也完成了结构归类。`PSMcluster0 0x0042` 是页图层容器：
`record_length = 72 + 4×member_count@26`，成员号从 `+30` 开始，且成员集合与同页 `0x0081` 命名图层集合
精确一致。`@22` 的 `member_anchor_ref` 也在全部 `1,195` 条容器中命中自身成员向量，并可解析为命名图层：
`NOZZLES` 为 `726` 条、`DwgTemplate` 为 `459` 条、`Default` 为 `10` 条。它只能标记为容器内图层锚点，
不能据此推断当前层、可见性、打印状态或几何。`0x0088` 是独立的页级 `Default` 命名对象，字段布局与 `0x0081` 不同，不能混入可见图层目录。
其 `page_parent_ref@26` 已在全部 `1,195` 条物理页记录中直接命中另一条 `0x0081 name=Default` 对象，证明
这是 Default 对象的包含关系，不是 Sheet 编号、可见图层或构件引用。
继续以该 `0x0081 Default` 对象在 `0x0042 member_refs` 中查找时，全部 `1,195/1,195` 条 `0x0088`
均唯一命中一个页面图层容器，因此链路已闭合为 `0x0088 -> 0x0081 Default -> 0x0042 -> Sheet`。此外，`0x0042`
可变成员向量后的固定 42 字节控制尾部中，第 2 个 uint32 也在 `1,195/1,195` 页直接等于同页 `0x0088 Default`
对象号，形成反向直连 `0x0042 -> 0x0088`；其余尾字段继续保留 raw，不能误作可见性/视口开关。这只说明
页面 Default 对象归属，不能把它误作页面可见层、构件、坐标或可绘制线条。
`0x0057` 是页或模板关联的显示图层配置对象，其 `sheet_stream_id@22` 直接回指 Sheet。短 `148/176` 字节
变体仅包含 `Default`/`DwgTemplate`；`1756` 字节变体每个 SHA 恰有一个并固定回指共享 `Sheet221`，罕见
`1916` 字节变体则位于物理页。两种长变体都携带受边界约束的 UTF-16 图层名，例如 `PIPES`、`DIMTEXT`、
`WELDS`、`MATLIST`、`Border`。

长变体的内部表已进一步解开：分别从 `1756@+142`、`1916@+126` 开始，格式为重复的
`<uint32 名称字符数><UTF-16 名称><uint16 图层目录编号>`，尾部另有固定的 `uint32=2` 终止值；前置数量
包含该终止值，故实际为 `82/91` 项。每项名称与相同 Sheet 的 `0x0042 -> 0x0081` 图层名集合逐项交叉后，
只有稳定保留层 `05` 未进入该表，表中没有额外名称。此前暂称 `state_raw` 的 `uint16` 已完成跨表验证：全部
`469` 条长配置记录（`459` 条 `1756` 模板、`10` 条 `1916` 物理页）中的每个条目都通过
`0x0057 -> Sheet -> 0x0042 member -> 0x0081` 链路，与对应命名图层的 `entry_id` 精确相等。它是图层目录
编号/顺序字段，不是显示、冻结、打印、颜色或几何状态。解析输出保留兼容字段 `state_raw`，并新增等值且语义
准确的 `entry_id_raw`。
在 `/管道` 的 `459` 份 SHA、`1,195` 张物理 Sheet 中，`0x0042` 与 `0x0088` 都恰好各有 `1,195` 条；
`0x0057` 已验证 `1,195` 条，全部回指有效 Sheet。它们证明页面/模板的图层状态路由，但还不能把原始字段
解释成可见性开关、视口、变换或几何绘制指令；绝不是法兰、焊点或需要绘制的管道图元。

`PSMcluster0` 内的 `0x0089` 不能与 `Unclustered Dynamic Attributes` 尾部同编号记录混为一谈。全库
`459` 份 SHA 共 `2,304` 条有界应用属性对象，外层均满足
`record_length = 20 + property_payload_length@24`，可分为 `_PastedGraphic`、`MSTN_GLOBALS`、`_ISO`、
`PipeLine Info` 四种应用元数据。全部 `919` 条 `MSTN_GLOBALS` 均有有界 `FileName` 文本，紧随其后的
值是 `.dgn` 模板源路径（本库均为 `...DowExampleBorders\\MYLARD.dgn`）；它只能作为 MicroStation
应用/模板来源追溯，不能作为 Sheet 坐标、UCI 或构件属性。仅有的两条 `PipeLine Info` 并不保存某个构件的实际值，而是声明
`PipeLine Reference`、`Fly Text`、`Unique Component Identifier`、`UCI Index`、`Element Tag` 五个属性标签。
`_PastedGraphic` 的资源桥必须严格限制：`1,377` 条中仅 `459` 条满足 `object_ref - 1` 精确等于现存
`JSite` 资源号，且全部指向带 `CONTENTS` 的 `JSite690`；另外 `918` 条没有任何此类资源流匹配，必须保留为
未绑定应用对象，不能因为数值相邻就画成位图或关联构件。
其中 `8` 条记录具有非零 `parent_object_ref@18`；`8/8` 精确命中有界 PSM 包围盒，`6/8` 还能在同 SHA 的
Sheet 中命中组合图形及其线段；其余 `2/8` 是此前遗漏的扩展 `0x004D` 文字，纳入该布局后也全部命中。
这证明它是“应用属性对象挂接到图形 PSM 对象”的关系，不能替代
已独立验证的动态 UCI -> Sheet 图元链，也不能由此新增、移动或改写图形。

`PSMcluster0 0x0075` 是每个 SHA 恰有一条的顶层文档目录记录（`record_length=113`）。它的三项受边界
UTF-16 字节长度表稳定为 `SiteObjects -> 2`、`PreferenceSet -> 3`、`Sheets -> 4`，全 `459` 个 SHA
完全一致。这些值是目录项编号，不是 Sheet 本地引用、UCI、构件编号或绘图命令；它说明部分低编号 PSM
关系实际属于文档组织层。

`PSMcluster0 0x0002` 是每个 SHA 恰有一条的固定 `PreferenceSet` 索引容器（长度 `768`、对象号 `256`）。
其中有 `25` 项八字节索引，前两个 `uint16` 为大端序、末尾 `uint32` 为小端序；全库存在三种索引内容变体，
但边界和项数均稳定。容器中可见应用偏好名称，不属于 ISO 的几何、UCI、Sheet 本地引用或绘图路径；各项
原始编号在未建立直接写入/读取链路前必须保持未命名。

`PSMcluster0 0x006C` 是每个 SHA 恰有一条的文档默认值包装记录。它固定嵌套 `0x0088 Default object=9 /
parent=8`，随后是 `0x0037 companion style object=10`；全 `459` 个 SHA 的边界和样式原始字段一致。因此
低编号 `10` 是嵌入式默认样式载荷，不能作为独立管道构件、Sheet 图元或需要绘制的 PSM 关系目标。

`PSMcluster0 0x0073` 是每个 SHA 恰有一条的固定 `515` 字节全局 `Background` site-object 容器。容器内有
一条受边界约束、八字节对齐的 `0x0076` 子记录，相对位置为 `64..144`。子记录 `+58` 的 UTF-16 字段全库均为
`Sketch`；紧随其后的 UTF-16 字段是可选源图号。全 `459` 个 SHA 中 `450` 个为空，`9` 个的值逐字等于 SHA
文件名去掉末尾版本段（例如 `N400P3A-HOSO-N419211-01-0.sha -> N400P3A-HOSO-N419211-01`）；其记录长度严格为
`307 + 2×图号字符数`，实际观测为 `307/345/349/353/355`。这证明它是 Background/Sketch 的文档元数据，不是
ISO 管道几何、构件编号或 SVG 绘制指令；其余数值区尚未证明语义，必须原样保留。

`PSMcluster0 0x0064` 也已完成排除性解码：它是每个 SHA 恰有一条的全零控制槽，字节形式固定为
`0x0064 + uint32(101) + 101 个零字节`，总长 `107` 字节。全 `459` 份 SHA 均严格匹配，只有流偏移因前置可选
文档数据而为 `589/591/593`。该槽没有对象号、文字、几何、构件身份或绘图含义，应仅作为文档完整性/保留占位
记录；不能尝试渲染或关联到 PCF。

文档级 `0x0065 + uint32(114)` 位置已经进一步解开为 `Section1` 输出页目录。`Section1`
（相对偏移 `89/91/93`）之后固定为 `02 01 00 00 00`、一个 uint32 页数，以及受长度约束的
`<uint16 UTF-16长度, Sheet1..SheetN, byte=1>` 顺序列表，末尾是 `Backgrounds`。`Backgrounds` 的末尾
字符 `s` 与紧随的 `0x0073 Background` 标记共享两个字节，这正是此前外层记录看似交叠的原因。全库
`459/459` 个 SHA 中，该目录页数均精确等于 `0x0042` 页面容器数减去共享模板 `Sheet221`：目录 `Sheet1`
对应实际首张 ISO `Sheet6`，后续 `Sheet2..N` 按容器顺序对应后续物理 Sheet 流。它是 ISO 输出页的**顺序目录**，
不是 OLE 流名、坐标、构件或绘图命令；`0x0065` 其余数值字段仍保留原始值。
