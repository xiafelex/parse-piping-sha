# 已登记的 SHA 解码限制

## Composite `primitive_type=0` 路径

状态：待解码，不进入当前默认渲染器。

现象：第一批十个 `SHA + PCF` 样本中，`2` 个法兰、`2` 个阀门、`1` 个弯头和 `1` 个仪表可以通过 `UCI -> graphic_ref -> PSM` 找到页面位置，但附近没有已解码的直线、圆弧或椭圆图元。

已确认：这些实体关联到 Shape2D 复合记录的 `primitive_type=0` 子引用。其记录中的范围与 PSM 位置相符，但直接将范围画成 SVG 方框会生成错误的大框，因此该范围不是最终可见轮廓。

已排除的错误做法：

- 不把 `type=0` 的范围直接渲染为矩形。
- 不用 PDF 图元或坐标反推 SHA。
- 不把 PCF 三维坐标直接投影到 ISO 图纸。

后续解码方向：找出 `type=0` 子引用对应的底层 Shape2D 路径和局部变换，再恢复成 SVG path。PCF 仅用于确认构件类别；位置、尺寸、朝向必须仍来自 SHA。
