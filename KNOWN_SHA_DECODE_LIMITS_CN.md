# 已登记的 SHA 解码限制

## Composite `primitive_type=0` 路径

状态：待解码，不进入当前默认渲染器。

现象：第一批十个 `SHA + PCF` 样本中，`2` 个法兰、`2` 个阀门、`1` 个弯头和 `1` 个仪表可以通过 `UCI -> graphic_ref -> PSM` 找到页面位置，但附近没有已解码的直线、圆弧或椭圆图元。

已确认：这些实体关联到 Shape2D 复合记录的 `primitive_type=0` 子引用。其记录中的范围与 PSM 位置相符，但直接将范围画成 SVG 方框会生成错误的大框，因此该范围不是最终可见轮廓。

### 当前处理决定

用户所称的 `type=-` 问题在当前代码和样本中统一登记为“复杂复合路径 / `primitive_type=0`（以及待确认的相邻负值类型）”。它已被单独记录，但暂不以猜测性矩形、直线或模板替代物强行绘制。后续需要按原始复合子记录、局部变换和可见性关系专项解码；在此之前，继续处理其他可以由多张 SHA 交叉验证的简单和中等图元规则。

已排除的错误做法：

- 不把 `type=0` 的范围直接渲染为矩形。
- 不用 PDF 图元或坐标反推 SHA。
- 不把 PCF 三维坐标直接投影到 ISO 图纸。

后续解码方向：找出 `type=0` 子引用对应的底层 Shape2D 路径和局部变换，再恢复成 SVG path。PCF 仅用于确认构件类别；位置、尺寸、朝向必须仍来自 SHA。

## PCF/SHA 候选关联

覆盖审计中，动态属性的 `graphic_ref` 仅以四字节小端序列出现在某个 `Sheet` 内时，不能作为构件已经绑定到该页面图元的证据。它可能刚好命中复合坐标字段。审计报告现在将此类条目标记为 `candidate-sheet-byte-occurrence`；它只用于排查，不能用来决定某个 type-0/type-2 子图元属于该 PCF 构件。提升为直接关联前，必须有已解码的 Sheet 引用字段或已验证的 PSM 父子关系。
