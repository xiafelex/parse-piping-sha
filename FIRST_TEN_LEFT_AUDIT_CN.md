# 第一批十个 SHA 左侧问题审计

本批选择 10 个已配对的 `SHA + PCF + PDF` 文件，实际覆盖 38 张物理 ISO 页。PCF 只用于确认文件配对；所有渲染规则、坐标和图元判断只来自 SHA。PDF 仅用于最后的人工视觉验收。

## 已验证修正

`Sheet6` 会把大量相同端点的线段同时写入普通两点记录和 `18/32` 记录族。原渲染器会把它们画两次，使主管、方框和构件细节变粗或发灰。

- 38 页中发现 15,565 条“端点完全相同”的跨记录族重复线段。
- 修正方法：保留普通两点记录；仅跳过与之完全同几何的 `18/32` 线段，仍保留该记录族独有的法兰、阀门和仪表细节。
- 左侧区域的 PDF 视觉 QA 平均差异从 `5.0078` 降至 `4.8503`。
- 32 页改善，6 页变化极小且略差；这些页没有或几乎没有可去重的 `Sheet6` 重复线段，后续仍需单独复核。

## 未接受的规则

对 `INSUL / CLASS / TRACE` 注释共审计到 489 条：

- 219 条的 PSM 字形包围盒与直接方框重叠。
- 270 条的文字锚点落在一个直接方框中，但 PSM 字形包围盒落在另一个局部布局区域。

虽然第二类看似可用文字锚点修正，38 页试验显示会把多行文字压到相邻方框，因此该规则已否决，未进入默认渲染器。后续需要按整组注释和方框父对象关系处理，不能逐条移动文字。

## 第二项已验证修正

部分带框工艺注释同时存在两套边线：普通/`18/32` 记录把边线放在文字锚点的局部坐标，复合记录则以同一个子图元编号给出与 PSM 字形一致的页面坐标。前者会造成叠框。

- 识别依据：两套线段的 `child primitive ref` 相同，但端点不同；复合记录组成闭合方框且位置与 PSM 字形一致。
- 修正方法：当复合闭合框已拥有某个子图元时，抑制普通及 `18/32` 记录族中同一子图元的局部副本，保留复合框。
- 全批左侧视觉 QA 平均差异从 `5.0078` 降至 `4.8474`，32/38 页改善。
- 这项规则只影响有明确复合闭合框子图元关系的边线，不移动文字，不从 PDF 读取坐标。

## 第三项已验证修正：工艺注释框

`CLASS / INSUL / TRACE` 工艺注释并不总是带有复合方框。第一批中一部分
注释框只存在于 `18/32` 两点记录族：同一框内至少有两条工艺注释的 Sheet
文字锚点，PSM 字形包围盒却位于偏移的局部布局空间。

- 识别依据：一个 `18/32` 闭合矩形内包含至少两条 `CLASS`、`INSUL` 或
  `TRACE` 的直接 Sheet 文字锚点；若有嵌套框，选最小的包含框。
- 渲染方法：保持该 `18/32` 框作为 SHA 原始几何；文字使用 Sheet 锚点作为
  基线，PSM 仅提供字高和字宽。
- 第一批 38 个目标页中，`295` 条工艺注释满足这个强 SHA 关系。
- 单页 `N400P3A-SCC-N504601-01-0 / Sheet6035` 的独立核对确认，多个
  `INSUL/TRACE` 双行框回到与 PDF 相同的相对位置。PDF 只用于验收；规则未
  使用 PDF 坐标、文字或矢量数据。

这不是全局文字平移：没有同框双注释证据的自由文字仍保留原有 PSM/样式规则。

## 下一步

1. 继续处理未命中该规则的自由文字、跨页连接注释和少量旋转/尺寸文字。
2. 继续补齐左侧法兰、支管、变径和仪表等局部构件的复合图元，不把 PDF 图形反写进 SHA 规则。
3. 对第一批十个样本重新逐页视觉验收；只有全部通过后才进入后续批次。
